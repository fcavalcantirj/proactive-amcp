# proactive-amcp Progress

## Task 1: validation — Identity pre-flight check (DONE)
- Added `validate_identity()` function to watchdog.sh, checkpoint.sh, full-checkpoint.sh, resuscitate.sh
- Function checks: (1) identity file exists, (2) `amcp identity validate` passes
- On failure: prints "FATAL: Invalid AMCP identity" and exits 1
- watchdog.sh: validates before entering monitoring loop (added AMCP_CLI/IDENTITY_PATH vars)
- checkpoint.sh: validates before creating tarball
- full-checkpoint.sh: validates before staging content
- resuscitate.sh: validates after arg parsing, before acquiring lock
- Added test helpers: create_mock_amcp(), create_valid_identity(), create_fake_identity()
- All scripts pass bash -n syntax check
- Structural grep confirms validate_identity in all 4 scripts

## Task 2: interface — proactive-amcp init (DONE)
- Created `scripts/proactive-amcp.sh` — main CLI entry point, dispatches subcommands
- Created `scripts/init.sh` — interactive setup (5 steps)
- Step 1: Checks ~/.amcp/identity.json exists + validates via `amcp identity validate`
- Step 2: If missing/invalid, creates identity via `amcp identity create --out`; backs up invalid identity
- Step 3: Sets up watchdog — systemd user service (preferred) or cron fallback
- Step 4: Sets up checkpoint schedule — systemd timer (preferred) or cron fallback
- Step 5: Writes config to ~/.amcp/config.json (Pinata JWT, notify target, watchdog interval, checkpoint schedule)
- Idempotent: detects existing config/identity/cron entries and updates in place
- Includes cron_to_oncalendar() for systemd timer conversion
- Guard pattern: `[ -x "$SCRIPT_DIR/notify.sh" ] && "$SCRIPT_DIR/notify.sh" ...`
- Both scripts pass `bash -n` syntax check
- File sizes: proactive-amcp.sh=41 lines, init.sh=461 lines (well under 800 limit)

## Task 3: secrets — config set/get for ~/.amcp/config.json (DONE)
- Created `scripts/config.sh` (234 lines) — implements `config set` and `config get` subcommands
- `config set <key> <value>`: dot-path notation (e.g. `pinata.jwt`, `notify.target`), auto type-coercion (bool/int)
- `config get [key]`: returns specific value or full config (with secrets redacted in display)
- Updated `scripts/proactive-amcp.sh` — added `config` subcommand routing via `exec "$SCRIPT_DIR/config.sh"`
- Migrated `notify.sh` to read from `~/.amcp/config.json` (AMCP config) with fallback to `~/.openclaw/openclaw.json`
  - `notify.target` → Telegram user ID
  - `notify.emailOnResurrect` → email enabled flag
  - `notify.emailTo` → email address
  - `notify.agentmailApiKey` → AgentMail API key
  - `notify.agentmailInbox` → AgentMail inbox address
- Added `warn_identity_secrets()` to checkpoint.sh, full-checkpoint.sh, watchdog.sh, resuscitate.sh, config.sh
  - Scans identity.json for common secret field names (pinata_jwt, solvr_api_key, etc.)
  - Prints warning + migration instructions to stderr
- Verification:
  - All 7 modified scripts pass `bash -n` syntax check
  - `grep` confirms zero scripts read secrets from identity.json
  - `grep` confirms notify.sh reads from `~/.amcp/config.json`
  - `grep` confirms `warn_identity_secrets` present in all 5 scripts (4 core + config.sh)
  - All files under 800 lines (largest: resuscitate.sh at 446 lines)

## Task 4: interface — proactive-amcp install (DONE)
- Created `scripts/install.sh` (377 lines) — non-interactive setup for fleet tools like openclaw-deploy
- Accepts CLI flags: `--pinata-jwt`, `--notify-target`, `--watchdog-interval`, `--checkpoint-schedule`, `--skip-services`
- All config values written via `config.sh set` (reuses existing config infrastructure)
- Identity: creates if missing, validates if present, backs up + replaces if invalid
- Services: systemd (preferred) or cron fallback, started automatically (no prompts)
- `--skip-services` flag: writes config + identity only, skips systemd/cron setup
- Structured JSON error output via `die_json()` — exits 1 with `{"error": true, "code": "...", "message": "..."}`
- Structured JSON success output: `{"success": true, "identity": "...", "config": "...", "services": "..."}`
- Guard pattern: `[ -x "$SCRIPT_DIR/notify.sh" ] && "$SCRIPT_DIR/notify.sh" ...`
- Updated `scripts/proactive-amcp.sh` — added `install` subcommand routing via `exec "$SCRIPT_DIR/install.sh"`
- Verification:
  - Both scripts pass `bash -n` syntax check
  - All 4 CLI flags present in install.sh (--pinata-jwt, --notify-target, --watchdog-interval, --checkpoint-schedule)
  - Config values written via 4 `config.sh set` calls (pinata.jwt, notify.target, watchdog.interval, checkpoint.schedule)
  - `proactive-amcp.sh` routes `install` → `install.sh` confirmed
  - File sizes: install.sh=377 lines, proactive-amcp.sh=51 lines (well under 800 limit)

## Task 5: validation — Checkpoint pre-validation for cleartext secrets (DONE)
- Created `scripts/scan-secrets.sh` (160 lines) — shared secret scanner, sourceable by checkpoint scripts
- Detects 11 secret patterns: GitHub PATs (classic + fine-grained), OpenAI keys, Solvr keys, AgentMail keys,
  Moltbook keys, webhook secrets, AWS access keys, JWT tokens (eyJ...), Telegram bot tokens ([0-9]+:AA...)
- `scan_for_secrets <dir> [force]`: scans directory recursively, skips binary/.git/node_modules/__pycache__
- On detection (no force): prints "REFUSED: Checkpoint contains cleartext secrets in: <files>" and exits 1
- On detection (force): prints loud warning but continues for emergency checkpoints
- Updated `checkpoint.sh`: added `--force` flag, sources scan-secrets.sh, calls scan_for_secrets before $AMCP_CLI
- Updated `full-checkpoint.sh`: added `--force` flag, sources scan-secrets.sh, calls scan_for_secrets after staging
- Replaced positional arg parsing in checkpoint.sh with proper for-loop arg parsing (supports --notify + --force)
- scan-secrets.sh also works standalone: `./scan-secrets.sh <directory> [--force]`
- Verification:
  - All 3 scripts pass `bash -n` syntax check
  - `grep` confirms scan_for_secrets called in both checkpoint.sh and full-checkpoint.sh
  - `grep` confirms --force flag parsed in both checkpoint scripts
  - `grep` confirms REFUSED message in scan-secrets.sh
  - All required patterns present: JWT (eyJ...), bot tokens ([0-9]+:AA...), API keys
  - File sizes: scan-secrets.sh=160, checkpoint.sh=265, full-checkpoint.sh=454 (all under 800)

## Task 6: testing — Integration test: fake identity rejection (DONE)
- Created `test/fake-identity.bats` (378 lines) — 8 bats integration tests
- Test fixture: openclaw-deploy-style identity (sha256 AID, flat secrets pinata_jwt/solvr_api_key, no KEL)
- Tests use `create_fake_identity()` from test_helper.sh + `create_mock_amcp 1` (validate fails)
- Smart mock amcp for init/install tests: fails validate on sha256 AID, succeeds on did:keri AID, creates valid identity on `identity create`
- Test 1: watchdog.sh with fake identity exits 1 with "FATAL: Invalid AMCP identity" before monitoring loop
- Test 2: checkpoint.sh with fake identity exits 1 before creating tarball (verified no .amcp files created)
- Test 3: full-checkpoint.sh with fake identity exits 1 before staging content
- Test 4: resuscitate.sh with fake identity exits 1 before attempting resurrection (no lock file created)
- Test 5: proactive-amcp init detects fake identity → backs up → replaces via `amcp identity create` → new identity has did:keri AID
- Test 6: config set writes to config.json, identity.json unchanged (content identical before/after)
- Test 7 (bonus): fake identity fixture validation — sha256 AID, flat secrets present, no KEL
- Test 8 (bonus): install.sh replaces fake identity non-interactively with --skip-services
- Key setup: export AMCP_CLI="$MOCK_BIN/amcp" (scripts use absolute path, not PATH)
- All scripts in sandbox (isolated from real gateway), mock notify.sh/crontab/systemctl
- Verification:
  - All 8 bats tests pass
  - All 16 scripts pass `bash -n` syntax check
  - File size: 378 lines (under 800 limit)
  - Structural grep confirms all 6 PRD test cases covered

## Task 7: solvr — Auto-register child Solvr account on first boot (DONE)
- Created `scripts/solvr-register.sh` (409 lines) — auto-registration of child Solvr accounts
- Step 1: Checks for existing SOLVR_API_KEY in 3 sources (env var, OpenClaw config, AMCP config)
  - If found: logs "Already registered" and exits 0
- Step 2: Checks for parentSolvrName in OpenClaw config and AMCP config
  - OC path: `skills.entries["proactive-amcp"].config.parentSolvrName`
  - AMCP path: `solvr.parentName`
- Step 3: If parentSolvrName exists (child): builds `{parentSolvrName}_child_{instance_name}`
  - Validates instance name (lowercase alphanumeric + underscore, max 32 chars)
  - Checks name availability via GET `/agents/{name}` (expects 404)
  - Falls back to suffix `_2` through `_5` if name taken
  - Registers via POST `/v1/agents/register` with parent key as Bearer auth
- Step 4: Stores returned API key in both AMCP config (`solvr.apiKey`) and OpenClaw config (`skills.entries.proactive-solvr.apiKey`)
- Step 5: If parentSolvrName missing (root): logs warning "Solvr features disabled until manual registration", exits 0
- Added `--instance-name` flag (defaults to sanitized hostname) and `--dry-run` flag
- Graceful handling: exits 0 with warning if no parent key available
- Updated `scripts/proactive-amcp.sh` — added `solvr-register` subcommand routing
- Guard pattern: `[ -x "$SCRIPT_DIR/notify.sh" ] && "$SCRIPT_DIR/notify.sh" ...`
- Created `test/solvr-register.bats` (308 lines) — 8 bats integration tests:
  - Test 1-3: Already registered (env, OC config, AMCP config) → exits 0
  - Test 4: Root agent (no parentSolvrName) → warns, exits 0
  - Test 5: Child agent auto-registers with correct protocol-08 naming
  - Test 6: Stores API key in AMCP config after registration
  - Test 7: Dry run shows plan without registering (curl NOT called)
  - Test 8: No parent key available → warns gracefully, exits 0
- Verification:
  - All 8 bats tests pass
  - All 17 scripts pass `bash -n` syntax check
  - File sizes: solvr-register.sh=409, proactive-amcp.sh=57 (all under 800)
  - Structural grep confirms all 6 PRD steps covered

## Task 11: bugfix — Fix full-checkpoint.sh staging exclusion (DONE)
- Staging copies ONLY identity.json into $STAGING_DIR/amcp/ (not rsync of ~/.amcp/)
- No cp of ~/.openclaw/openclaw.json into staging (secrets extracted by extract_all_secrets())
- No cp of ~/.openclaw/auth-profiles.json into staging (tokens extracted by extract_all_secrets())
- extract_config_metadata() writes redacted structural metadata to $STAGING_DIR/openclaw/config-metadata.json
- scan_for_secrets passes without --force on staging (config secrets never enter staging)
- Verification:
  - bash -n scripts/full-checkpoint.sh passes
  - File size: 523 lines (under 800)
  - No rsync of ~/.amcp/ in staging section
  - No cp of openclaw.json or auth-profiles.json into staging
  - Only identity.json copied to staging

## Task 12: bugfix — Config-metadata.json extraction (DONE)
- Created extract_config_metadata() function in full-checkpoint.sh (lines 312-377)
- Reads ~/.openclaw/openclaw.json via python3, applies recursive redaction
- SECRET_KEY_NAMES: apikey, api_key, apiKey, secret, password, jwt, token, botToken, bot_token, mnemonic, keyringPassword, key, private_key, privateKey
- SECRET_PATTERNS: 7 regexes (JWT, sk-*, ghp_*, solvr_*, am_*, Telegram bot tokens, AWS AKIA)
- Preserves structural info: gateway.port, skill names, agent workspace path
- Redacts all secret values to "[REDACTED]"
- Output: $STAGING_DIR/openclaw/config-metadata.json
- Verification:
  - gateway.port preserved (not in SECRET_KEY_NAMES)
  - botToken, apiKey values redacted
  - No secret patterns pass through

## Task 13: testing — Integration tests for staging exclusion (DONE)
- Created test/full-checkpoint.bats (284 lines) — 10 bats integration tests
- Test 1: staging excludes ~/.amcp/config.json
- Test 2: staging includes ~/.amcp/identity.json
- Test 3: full-checkpoint succeeds without --force when config has secrets (exit 0, no REFUSED)
- Test 4: full-checkpoint refuses when workspace content has hardcoded API key (exit 1, REFUSED)
- Test 5: --force bypasses workspace secret check with warning
- Test 6: staging contains openclaw/config-metadata.json in dry-run output
- Test 7: config-metadata.json does NOT contain botToken values
- Test 8: config-metadata.json does NOT contain API keys
- Test 9: full pipeline: stage + scan + create succeeds with secrets in config
- Test 10: full pipeline: refuses if workspace has hardcoded secret
- Verification:
  - All 10 bats tests pass
  - File size: 284 lines (under 800)

## Task 14: pinning — Create pin-to-solvr.sh script (DONE)
- Created `scripts/pin-to-solvr.sh` (136 lines) — thin wrapper around Solvr CLI
- Uses Solvr CLI `solvr pin add-file`, does NOT reimplement API calls
- Accepts $1=file_path (required), $2=name (optional, defaults to filename)
- Reads SOLVR_API_KEY from env or config.json (solvr.apiKey or pinning.solvr.apiKey)
- Checks for `solvr` CLI installation via command -v, shows install instructions if missing
- Parses CID from CLI output (regex for Qm/bafy CID, fallback to JSON parse)
- `--dry-run` flag shows what would be pinned without executing
- On success: echoes CID, exit 0
- On failure: echoes error, exit 1
- Verification:
  - bash -n syntax check passes
  - File size: 136 lines (under 800)

## Task 3 (index 3): pinning — Add Solvr as alternative pinning provider (DONE)
- Added `PINNING_PROVIDER` config reading to full-checkpoint.sh and checkpoint.sh
- Config path: `pinning.provider` in ~/.amcp/config.json ('pinata' | 'solvr' | 'both')
- Extracted pin_to_pinata() and pin_to_solvr() functions in both scripts
- Provider routing via case statement: solvr, both, pinata (default)
- 'both' mode: calls Pinata first, then Solvr; logs CID mismatch warning if different
- 'both' mode: tolerates individual provider failures (|| true)
- Updated last-checkpoint.json in full-checkpoint.sh: adds pinningProvider, pinataCid, solvrCid fields
- Updated notification message to include provider info
- pin_to_solvr() delegates to pin-to-solvr.sh (Solvr CLI wrapper)
- Verification:
  - bash -n syntax check passes on both scripts
  - File sizes: full-checkpoint.sh=584, checkpoint.sh=315 (under 800)
  - PINNING_PROVIDER present in both scripts
  - pin_to_solvr present in both scripts

## Task 31: pinning — Update config set to support solvr.apiKey (DONE)
- config.sh already supports arbitrary dot-paths (solvr.apiKey works out of the box)
- Added validate_after_set() function with post-set validation for solvr.apiKey and pinning.solvr.apiKey
- Key format validation: warns if key doesn't match 'solvr_...' or agent key pattern
- Key validity test: calls GET /v1/me with the API key, reports account name on success
- Graceful degradation: warnings on validation failure, never blocks the set operation
- Verification:
  - bash -n syntax check passes
  - File size: 272 lines (under 800)

## Task 36: pinning — Update install to accept --solvr-api-key flag (DONE)
- Added --solvr-api-key <key> flag to install.sh arg parsing
- Added --pinning-provider <provider> flag with validation ('pinata'|'solvr'|'both')
- Added --solvr-base-url <url> flag for custom Solvr API endpoint
- install_config() writes: solvr.apiKey, pinning.solvr.apiKey, pinning.provider, pinning.solvr.baseUrl
- Created install_solvr_cli() function: installs Solvr CLI when provider is 'solvr' or 'both'
  - Checks command -v solvr first, skips if already installed
  - Falls back to curl -sL solvr.dev/install.sh | bash
  - Verifies installation with solvr --version
  - Warns (no abort) if installation fails
- Updated help text with new flags and examples
- Updated main() to call install_solvr_cli after install_solvr_skill
- Verification:
  - bash -n syntax check passes
  - File size: 484 lines (under 800)
  - All 3 new flags present in arg parsing and help text

## Task 37: pinning — Update resuscitate.sh to try Solvr gateway first (DONE)
- Created fetch_from_gateways() function with 4 gateways in priority order:
  1. Solvr (https://ipfs.solvr.dev/ipfs/{CID})
  2. Pinata (https://gateway.pinata.cloud/ipfs/{CID})
  3. IPFS.io (https://ipfs.io/ipfs/{CID})
  4. Cloudflare (https://cloudflare-ipfs.com/ipfs/{CID})
- Added --gateway flag to override priority: resuscitate.sh --gateway solvr|pinata|ipfs.io|cloudflare
- Preferred gateway tried first, then falls through to remaining gateways
- Each gateway attempt logged: "Checkpoint retrieved from {name} gateway"
- Uses curl -sf (silent + fail on HTTP errors) with --max-time 120
- Validates downloaded file is non-empty with [ -s "$output" ]
- Replaced hardcoded Pinata curl call in try_rehydrate() with fetch_from_gateways()
- Verification:
  - bash -n syntax check passes
  - File size: 501 lines (under 800)
  - fetch_from_gateways, PREFERRED_GATEWAY, and ipfs.solvr.dev all present

## Task 42: testing — Integration tests for Solvr pinning (DONE)
- Created `test/solvr-pinning.bats` (399 lines) — 8 bats integration tests
- Test 1: pin-to-solvr.sh with valid file returns CID (mock solvr CLI)
- Test 2: pin-to-solvr.sh with missing API key fails gracefully (exit 1, "No Solvr API key")
- Test 3: full-checkpoint.sh with provider=solvr uploads to Solvr (pinningProvider=solvr in last-checkpoint.json)
- Test 4: full-checkpoint.sh with provider=both uploads to both (Pinata + Solvr)
- Test 5: resuscitate.sh tries Solvr gateway first when fetching by CID (ipfs.solvr.dev in curl.log)
- Test 6: resuscitate.sh --gateway flag overrides default order
- Test 7: last-checkpoint.json stores pinataCid and solvrCid when provider=both
- Test 8: pin-to-solvr.sh --dry-run shows plan without calling Solvr CLI
- Fixed pin-to-solvr.sh: progress messages to stderr (only CID on stdout), broadened CID regex to match bafk*
- Fixed pin_to_solvr() in checkpoint.sh and full-checkpoint.sh: capture only stdout (not stderr)
- Fixed full-checkpoint.bats mock curl: use quoted heredoc to preserve JSON quotes
- Verification:
  - All 8 new tests pass
  - All 75 tests across full suite pass (no regressions)
  - File size: 399 lines (under 800)

## Task: documentation — Document Solvr IPFS infrastructure in README (DONE)
- Added 'Storage Providers' section to README.md
- Documented Solvr as primary (free for AMCP agents), Pinata as fallback
- Included solvr-ipfs-01 details: gateway URL, Peer ID, Kubo version, P2P port
- Explained provider selection: pinata (default), solvr, both
- Documented retrieval priority: Solvr > Pinata > IPFS.io > Cloudflare
- Linked to Solvr registration: https://solvr.dev/register
- Added setup examples for Solvr and redundant pinning

## Task: recovery — Hook recreate-venvs.sh into resuscitate.sh (DONE)
- Added recreate-venvs.sh call in try_rehydrate() after workspace restore + secret injection
- Placed BEFORE gateway restart (temp file cleanup + restart gateway)
- Uses guard pattern: [ -x "$SCRIPT_DIR/recreate-venvs.sh" ] to skip if not present
- Manifest path: $CONTENT_DIR/memory/venvs-manifest.json (dynamic)
- Output piped to $RECOVERY_LOG via tee
- Uses || echo "WARN:" to prevent venv failures from aborting resurrection
- Verification:
  - bash -n syntax check passes
  - File size: 508 lines (under 800)
  - All 75 tests pass (no regressions)

## Task: pinning — Add 'proactive-amcp migrate-pins' (DONE)
- Created `scripts/migrate-pins.sh` (472 lines) — transfer historical checkpoints from Pinata to Solvr
- Collects unique CIDs from `~/.amcp/checkpoints/*.json` and `last-checkpoint.json`
- For each CID: checks if already pinned on Solvr via gateway HEAD request
- If not pinned: POST `/v1/pins` with CID and name to Solvr API
- Tracks progress in `~/.amcp/migration-status.json` (per-CID status + summary counts)
- `--unpin-pinata` flag: removes from Pinata after Solvr pin confirmed (DELETE unpin API)
- `--dry-run` flag: shows what would be migrated without making changes
- Skips already-migrated CIDs (from status file) for idempotent re-runs
- wait_for_pin() polls Solvr gateway with 5-minute timeout
- Verification step at end: checks all CIDs accessible via Solvr gateway
- Added `migrate-pins` subcommand to proactive-amcp.sh CLI dispatcher
- Verification:
  - bash -n syntax check passes on both scripts
  - File sizes: migrate-pins.sh=472, proactive-amcp.sh=69 (under 800)
  - All 6 PRD steps verified via structural grep

## Task: pinning — Add redundant pinning mode (DONE)
- Already implemented in Phase 1 (full-checkpoint.sh and checkpoint.sh)
- pinning.provider='both' calls Pinata first, then Solvr with || true
- last-checkpoint.json stores pinataCid and solvrCid fields
- CID mismatch detection present (logs warning if CIDs differ)
- Both-fail case detected and logged
- Verified in test/solvr-pinning.bats (Tests 4 and 7)

## Task: ontology — Compute ontology graph CID (DONE)
- Added compute_ontology_cid() function to full-checkpoint.sh (after extract_all_secrets)
- Reads $CONTENT_DIR/memory/ontology/graph.jsonl if exists
- Computes SHA-256 hash, formats as CIDv1 raw (version=0x01, codec=0x55, multihash sha256=0x12, len=0x20)
- Base32lower encode with 'b' multibase prefix (deterministic)
- Uses python3 stdlib only (hashlib + base64), no external dependencies
- Stores ontologyGraphCID in last-checkpoint.json (optional, only if ontology exists)
- Graceful skip: if graph.jsonl missing, ONTOLOGY_GRAPH_CID remains empty, field omitted
- Verification:
  - bash -n syntax check passes
  - File size: 624 lines (under 800)
  - compute_ontology_cid function and ontologyGraphCID field present

## Task: ontology — Document reconstruction sequence (DONE)
- Created RECONSTRUCTION.md (~150 lines) documenting the Uncanny Seam and canonical loading order
- 8-step sequence: SOUL.md -> USER.md -> ontology schema -> ontology graph -> MEMORY.md -> daily notes -> TOOLS.md -> [SEAM]
- Phenomenological rationale: each layer provides interpretive context for the next
- Agent guidance section: "If you are reading this during resurrection, you are currently between steps 1-7"
- References LumenNox (Aclawdemy, "The Phenomenology of Forgetting") and ONTOLOGY-INTEGRATION-CONTEXT.md Part 4.5
- Added Memory Architecture section to README.md linking to RECONSTRUCTION.md

## Task: learning — Initialize learning storage (DONE)
- Added setup_learning_storage() to init.sh (interactive) and install_learning_storage() to install.sh (non-interactive)
- Creates memory/learning/ directory in workspace with:
  - problems.jsonl — empty append-only log for Problem entities
  - learnings.jsonl — empty append-only log for Learning entities
  - stats.json — zeroed statistics (total_problems, total_solved, solved_post_recovery, total_learnings, last_updated)
- Idempotent: skips if directory and stats.json already exist
- full-checkpoint.sh: workspace rsync already includes memory/learning/ ; added log line confirming inclusion
- resuscitate.sh: added JSONL validation after workspace restore
  - Validates each line of problems.jsonl and learnings.jsonl is valid JSON
  - Validates stats.json is valid JSON
  - Logs warnings on corruption but does not block resurrection
- Verification:
  - bash -n syntax check passes on all 4 scripts
  - File sizes: init.sh=505, install.sh=517, resuscitate.sh=546, full-checkpoint.sh=632 (all under 800)

## Task: learning — Problem CRUD + Learning CRUD operations (DONE)
- Created `scripts/learning.py` (440 lines) — Problem and Learning entity CRUD operations
- Append-only JSONL storage: each operation appends a record, get/list replays the log
- Problem operations: create_problem, update_problem, get_problem, list_problems, close_problem
  - prob_{uuid8} ID generation
  - Status: open, stuck, solved, abandoned
  - Append-only: update appends op=update, close appends op=close
  - stats.json: total_problems incremented on create, total_solved on close
- Learning operations: create_learning, verify_learning, get_learning, list_learnings
  - learn_{uuid8} ID generation
  - Confidence: tentative (default), verified (after human confirmation)
  - Tags support, source_problem link
  - Auto-close linked problem when learning created with --source-problem
  - stats.json: total_learnings incremented on create
- CLI dispatcher: proactive-amcp problem create|update|get|list|close
- CLI dispatcher: proactive-amcp learning create|verify|get|list
- LEARNING_DIR env var overridable, defaults to $workspace/memory/learning
- Functional tests verified: create -> list -> close cycle, learning -> auto-close problem, stats updated
- Verification:
  - python3 -c py_compile passes
  - File size: 440 lines (under 800)
  - proactive-amcp.sh routes problem/learning commands

## Task: learning — SKILL.md trigger matching + command prefix triggers (DONE)
- Updated SKILL.md version to 0.7.1
- Added trigger keywords to frontmatter: remember, forgot, forgetting, stuck, learned, solved, never forget, /remember, /stuck, /learned
- Added "Learning Capture" section with two modes:
  - Command prefixes: /remember, /stuck, /learned (with --solves support)
  - Natural language: "Remember that...", "I keep forgetting...", "I can't figure out...", "I finally solved...", "Never forget:..."
- Documented how agent routes patterns to proactive-amcp CLI commands
- Added querying examples (list, get, close)
- Verification:
  - SKILL.md: 446 lines (under 800)
  - All 5 natural language patterns documented
  - All 3 command prefix patterns with examples

## Task: learning — Human verification flow (DONE)
- confidence=tentative is default for new Learnings (already implemented)
- verify_learning() promotes confidence to verified, sets verified_at + human_verified
- Added verified_learnings counter increment in stats.json on verify
- Added check_unverified(days=7) function: finds tentative learnings older than N days, logs warnings
- Added CLI: proactive-amcp learning check-unverified [--days N]
- Verification:
  - python3 py_compile passes
  - File size: 481 lines (under 800)

## Task: learning — Hybrid resurrection behavior (DONE)
- Created `scripts/generate-problem-summary.py` (128 lines) — reads problems.jsonl, outputs markdown summary
- Summary includes: problem ID, status, description, attempt count, created date
- Instruction: "do NOT auto-attempt all at once"
- Added `surface_open_problems()` to resuscitate.sh — calls generate-problem-summary.py after each recovery success
- Writes summary to `~/.amcp/open-problems-summary.md` for agent context injection
- Config toggle: `learning.resurrection.surfaceProblems` (default: true, checked by surface_open_problems)
- Called at all 3 success points: restart, config_fix, rehydrate
- Uses guard pattern: checks if generate-problem-summary.py exists before calling
- Verification:
  - bash -n resuscitate.sh passes, python3 compile passes
  - File sizes: resuscitate.sh=581, generate-problem-summary.py=128 (under 800)

## Task: ontology-evolution — Memory evolution engine (DONE)
- Created `scripts/memory-evolution.sh` (279 lines) — orchestrates evolution: validate identity, read graph, compute similarity, add relations, log chain
- Created `scripts/compute-entity-similarity.py` (185 lines) — semantic similarity using Levenshtein distance (0.4 weight) + keyword overlap (0.6 weight)
- Evolution trigger: --entity-id for single entity, --all-new for all unevolved entities
- Finds top-N (default 3) similar entities above threshold (default 0.75)
- Adds bidirectional 'related_to' relations to graph.jsonl
- Logs to ~/.amcp/memory/evolution.jsonl: {timestamp, trigger_entity_id, related_entity_id, similarity_score, relation_type, reasoning}
- Integrated into full-checkpoint.sh after graph staging, before size calculation
- --skip-evolution flag on full-checkpoint.sh to bypass
- --dry-run shows what would be linked without modifying files
- Environment: EVOLUTION_THRESHOLD (default: 0.75), EVOLUTION_MAX_RELATIONS (default: 3)
- Verification:
  - bash -n / python3 py_compile passes on all scripts
  - File sizes: memory-evolution.sh=279, compute-entity-similarity.py=185, full-checkpoint.sh=647 (all under 800)
  - surface_open_problems called at all 3 recovery success points

## Task: learning — Self-detect trigger (DONE)
- Created `scripts/detect-failure.py` (206 lines) — scans agent text for failure patterns, auto-creates Problems
- 7 failure patterns across 4 categories:
  - api_error: HTTP 4xx/5xx with status names, status_code/HTTP/error codes
  - knowledge_gap: "I don't know how to", "I need help with", "I'm not sure/unable to/struggling to"
  - error_signal: FATAL/CRITICAL/ERROR: messages
  - auth_failure: authentication/auth/token/api_key failed/expired/invalid/rejected/revoked
- One detection per category per scan (prevents duplicate noise)
- Debounce: checks existing open self-detect problems within configurable window (default 24h)
  - Token-based Jaccard similarity (>0.5 overlap = duplicate)
- Creates Problems via learning.py subprocess with source=self-detect
- Logs all detections to memory/learning/detections.log (JSONL)
- Config toggle: learning.selfDetect.enabled (default: true), learning.selfDetect.debounceHours (default: 24)
- Input modes: --input <file_or_stdin> or --scan-sessions (last 24h session transcripts)
- --dry-run shows detections without creating Problems
- Verification:
  - python3 py_compile passes
  - Functional tests: 3 patterns detected, debounce works, config disable works, detections.log populated
  - File size: 206 lines (under 800)

## Task: temporal — Temporal query interface (DONE)
- Created `scripts/temporal-queries.py` (238 lines) — cross-checkpoint entity history queries
- 3-level temporal storage:
  - Level 1: Current graph.jsonl (live entities)
  - Level 2: Checkpoint snapshots in ~/.amcp/checkpoints/ (referenced by CID)
  - Level 3: Temporal index at ~/.amcp/memory/temporal-index.jsonl
- Temporal index format: {entity_id, checkpoint_cid, timestamp, version_hash, properties_snapshot}
- build-index: extracts entities from graph.jsonl, computes version_hash (sha256), skips unchanged
- history <entity_id>: full evolution timeline (index + current graph if changed)
- query <entity_id> --start/--end: filter by date range
- Integrated into full-checkpoint.sh: builds temporal index after evolution engine, before size calc
- Added temporal-query + detect-failure subcommands to proactive-amcp.sh CLI
- TEMPORAL_INDEX_PATH env var overridable (default: ~/.amcp/memory/temporal-index.jsonl)
- Verification:
  - python3 py_compile passes, bash -n passes on all modified scripts
  - Functional test: entity v0.1 in checkpoint-1, v0.2 in checkpoint-2 → history shows both with timestamps
  - Time range query correctly filters by date
  - File sizes: temporal-queries.py=238, full-checkpoint.sh=657, proactive-amcp.sh=93 (all under 800)

## Task: ontology — Include memory/ontology/ in full-checkpoint.sh (DONE)
- Already implemented: workspace rsync in full-checkpoint.sh includes memory/ontology/ directory
- Log line at L440-441 confirms ontology inclusion with JSONL file count
- scan-secrets.sh does not exclude .jsonl files — ontology entities don't match secret patterns
- resuscitate.sh restores memory/ontology/ via cp -r of extracted checkpoint content
- No additional code changes needed — marked passes=true

## Task: testing — Integration tests for venv recovery (DONE)
- Created `test/venv-recovery.bats` (6 tests)
- Test 1: All venvs functional → skips all (fast path)
- Test 2: Venv missing → creates it with mock python3 -m venv
- Test 3: Broken venv (pip fails) → recreates it
- Test 4: No manifest file → exits 0 with "No venvs manifest" skip message
- Test 5: Invalid JSON manifest → exits non-zero (script has set -e)
- Test 6: resuscitate.sh includes recreate-venvs.sh guard pattern
- All 6 tests pass

## Task: ontology — SOUL.md drift detection + integration tests (DONE)
- Fixed full-checkpoint.sh to write soulHash to last-checkpoint.json (was computed but never stored)
- Added `soul_hash` variable to the STAGE 5 python3 write block alongside ontologyGraphCID
- Created `test/soul-drift.bats` (6 tests):
  - Test 1: Initial checkpoint with SOUL.md stores soulHash in last-checkpoint.json, no drift alert
  - Test 2: Minor SOUL.md change (3 lines added, estimate=1) logs severity=minor, no notification
  - Test 3: Major SOUL.md change (120 lines, estimate=24) logs severity=major, triggers notification
  - Test 4: No SOUL.md present → no soulHash in last-checkpoint.json, no error
  - Test 5: SOUL.md unchanged across 3 checkpoints → no drift alerts logged
  - Test 6: config.json notify.enableSoulDrift=false → drift logged but no notification sent
- Verification:
  - bash -n full-checkpoint.sh passes
  - File size: 742 lines (under 800)
  - All 6 soul-drift tests pass
  - No regressions across full suite (84/87 pass, 3 pre-existing solvr-pinning failures)

## Task: testing — Integration tests for learning system (DONE)
- Created `test/learning-system.bats` (10 tests):
  - Test 1: /stuck (problem create) stores Problem in problems.jsonl with status=open, stats updated
  - Test 2: /learned (learning create --source-problem) links Learning to Problem, auto-closes Problem
  - Test 3: Natural language "Remember that X" creates Learning with insight and confidence=tentative
  - Test 4: detect-failure.py on 401 error creates Problem with source=self-detect
  - Test 5: Duplicate Problem detection — same error within 24h debounced (no duplicate created)
  - Test 6: Learning data survives checkpoint + restore cycle (copy, clear, restore, verify)
  - Test 7: generate-problem-summary.py surfaces open problems, excludes solved ones
  - Test 8: Human verification flow — verify changes confidence to verified, stats updated
  - Test 9: Stats accurate after mixed operations (3 problems, 2 learnings, 1 solved, 1 abandoned)
  - Test 10: solved_post_recovery — solving pre-resurrection problem increments counter
- Verification:
  - All 10 tests pass
  - No regressions (same 3 pre-existing solvr-pinning failures)

## Task: ontology-evolution — Integration tests for memory evolution (DONE)
- Created `test/memory-evolution.bats` (7 tests):
  - Test 1: Similar entities get related_to bidirectional relations in graph.jsonl
  - Test 2: Evolution log records entry with similarity_score >= 0.75
  - Test 3: Unrelated entity (different domain) gets no relations added
  - Test 4: EVOLUTION_THRESHOLD=0.95 prevents moderate-similarity matches
  - Test 5: EVOLUTION_MAX_RELATIONS=1 limits to top-1 relation only (2 lines: forward+backward)
  - Test 6: --dry-run shows "[DRY RUN]" without modifying files
  - Test 7: Evolution chain — C->B->A triggers tracked in evolution.jsonl across runs
- Test entity design: carefully tuned keyword overlap to produce scores above/below threshold
  - Similar entities: ~0.88 combined (high keyword + levenshtein overlap)
  - Unrelated entities: <0.3 combined (no keyword overlap)
- Verification:
  - All 7 tests pass
  - No regressions (same 3 pre-existing solvr-pinning failures)

## Task: ontology — Validate ontology on resurrection (DONE)
- Created `scripts/validate-ontology.py` (195 lines) — JSONL graph schema validation
  - Validates: valid JSON per line, required fields (id, type, properties for entities; from_id, relation_type, to_id for relations)
  - Relation integrity: from_id and to_id must reference existing entities
  - Acyclic check: 'blocks' relations must not form cycles (DFS cycle detection)
  - Output: JSON {valid, entity_count, relation_count, errors[]}
- Integrated into resuscitate.sh: calls validate-ontology.py after learning validation, before venvs
  - Guard pattern: only runs if graph.jsonl exists AND validate-ontology.py exists
  - Non-blocking: warnings logged to RECOVERY_LOG, resurrection continues on validation failure
- Verification:
  - python3 py_compile passes
  - bash -n resuscitate.sh passes, 588 lines (under 800)

## Task: testing — Integration tests for ontology checkpoint (DONE)
- Created `test/ontology-checkpoint.bats` (6 tests):
  - Test 1: Checkpoint with ontology includes memory/ontology/ in staging output
  - Test 2: last-checkpoint.json includes ontologyGraphCID (starts with 'b' multibase prefix)
  - Test 3: validate-ontology.py validates valid graph (3 entities, 1 relation)
  - Test 4: Corrupt graph (invalid JSON + dangling relation) reports errors but doesn't crash
  - Test 5: No ontology directory → no ontologyGraphCID in last-checkpoint.json (backward compatible)
  - Test 6: CID determinism — same graph produces identical CID across two runs
- Verification:
  - All 6 tests pass
  - No regressions (same 3 pre-existing solvr-pinning failures)

## Task: temporal — Integration tests for temporal queries (DONE)
- Created `test/temporal-queries.bats` (8 tests):
  - Test 1: Two versions across checkpoints — query returns both
  - Test 2: Time-range filter (start after checkpoint-1) returns only checkpoint-2
  - Test 3: Full history ordered chronologically (first < second timestamp)
  - Test 4: Unmodified entity shows single entry (build-index skips unchanged)
  - Test 5: Non-existent entity returns "No history" message, exit 0
  - Test 6: Missing temporal index degrades to current graph (checkpoint_cid="current")
  - Test 7: No ontology graph → build-index reports "No graph", exits 0
  - Test 8: Date-based snapshot query for 2026-02-15 returns correct version
- Verification:
  - All 8 tests pass
  - 115 total tests pass, same 3 pre-existing solvr-pinning failures

## Task: learning — Track and report learning metrics (index 46) (DONE)
- Created `scripts/learning-report.py` (148 lines, under 150 limit) — generates human-readable learning report
- Metrics computed from JSONL replay: total_problems, total_learnings, solve_rate, avg_attempts_to_solve
- Self-improvement rate: solved_post_recovery / total_solved * 100 (from stats.json)
- Breakdowns: problems_by_status (open/stuck/solved/abandoned), learnings_by_confidence (tentative/verified), problems_solved_by_source
- CLI: `proactive-amcp learning report [--output FILE] [--learning-dir DIR] [--json]`
- Markdown report format with ## Summary, ## Problems by Status, ## Learnings by Confidence, ## Problems Solved by Source
- JSON output mode: `--json` for raw metrics as JSON object
- --output flag writes to file instead of stdout
- LEARNING_DIR env var overridable (same pattern as learning.py)
- Routing added in proactive-amcp.sh: `learning report` dispatches to learning-report.py
- Verification:
  - python3 py_compile passes
  - File size: 148 lines (under 150)
  - Tested with 5 problems (3 solved, 1 open, 1 abandoned), correct solve rate 60%, self-improvement rate 66.7%
  - Empty data produces valid report with zeroes and "(none)" sections
  - CLI routing via proactive-amcp learning report --json works

## Task: ontology — Typed pruning policy framework (index 47) (DONE)
- Created `scripts/prune-ontology.py` (260 lines, under 300 limit) — typed pruning policies for ontology graph
- Config schema: `pruning.policies` in ~/.amcp/config.json with per-entity-type rules
  - Example: `{ "Event": { "ttl": "30d", "preserve_relations": true }, "Task": { "ttl": null, "prune_if": "status == 'done' AND updated < 90d" }, "Person": { "ttl": null } }`
- TTL parsing: supports h (hours), d (days), w (weeks), m (months), y (years)
- Conditional expressions: `field == 'value'`, `field != 'value'`, `field < TTL` (temporal)
  - Conjunctions with AND (all clauses must match)
- Pruning logic: parse JSONL → identify matching entities → remove + handle relations → atomic write (.pruned then rename)
- prune_if takes priority over ttl: null — Tasks with `prune_if` can be pruned even with `ttl: null`
- Entities with only `ttl: null` (no prune_if, e.g. Person) are never pruned
- Relation preservation: if ANY policy has `preserve_relations: true`, relation stubs kept with `deleted_at` timestamp
- `--dry-run` mode outputs JSON summary without modifying files
- `--config` and `--graph` flags for path overrides
- No policies configured → exits 0 with "no_policies" message
- Added `prune` subcommand to proactive-amcp.sh CLI dispatcher
- Verification:
  - python3 py_compile passes
  - File size: 260 lines (under 300)
  - Tested: Event >30d pruned, Event <30d kept, Person (ttl:null) kept, Task done+old pruned, Task done+recent kept, Task open kept
  - Relation stubs have deleted_at timestamp when preserve_relations=true
  - Dry-run outputs correct summary
  - CLI routing via `proactive-amcp prune --dry-run` works
  - 115 tests pass, same 3 pre-existing solvr-pinning failures

## Task: solvr-integration — Search Solvr before attempting any recovery fix (index 53) (DONE)
- Created `scripts/solvr-integration.sh` (268 lines) — extracted Solvr functions from resuscitate.sh
- `_extract_error_signature()`: extracts error context from gateway logs or systemd journal, condenses to search query (max 100 chars)
- `solvr_search_problem()`: searches Solvr API with error context, returns JSON with matching problems+approaches
- Reads SOLVR_API_KEY from env or config.json (solvr.apiKey, pinning.solvr.apiKey)
- Called BEFORE any recovery attempt in main() flow
- Logs: "Solvr: Found N similar problems" or "Solvr: No matches"
- Verification:
  - bash -n passes on both scripts
  - File sizes: solvr-integration.sh=268, resuscitate.sh=622 (both under 800)
  - All 118 tests pass, same 3 pre-existing solvr-pinning failures

## Task: solvr-integration — Try Solvr solutions before generic recovery (index 54) (DONE)
- `try_solvr_solutions()`: iterates problems from search, fetches approaches, tries succeeded ones
- Heuristic method matching: *restart*|*systemctl* -> try_restart_gateway, *config*|*restore* -> try_fix_config
- Skips rehydrate suggestions (too heavy for first pass)
- On success: returns 0 (recovery done via Solvr)
- On failure: calls solvr_post_failed_approach() on the existing problem
- Integrated in main(): Step 1b after Solvr search, before generic recovery

## Task: solvr-integration — Create Solvr problem if no match found (index 55) (DONE)
- `solvr_create_problem()`: POST /v1/posts with type=problem, title, description, tags
- Title: "Agent death: {AGENT_NAME} - {error_sig:0:50}" (max 80 chars)
- Description: "Agent {AGENT_NAME} experienced death. Error context: {error_sig:0:450}"
- Tags: ["openclaw", "death", "auto-generated"]
- Stores problem_id in ~/.amcp/current-death-problem.json for approach tracking
- Called in main() Step 1c: after Solvr search returns empty, BEFORE starting generic recovery

## Task: solvr-integration — Update Solvr approach after each recovery attempt (index 56) (DONE)
- `solvr_start_approach()`: POST /v1/problems/{id}/approaches with angle and method
- Stores approach_id in ~/.amcp/current-approach.json
- `solvr_update_approach()`: PATCH /v1/approaches/{id} with status (succeeded/failed)
- Called for each recovery method: restart, config_fix, rehydrate
- Start called BEFORE each attempt, update called AFTER result
- Graceful: no-op if no API key or no problem_id

## Task: solvr-integration — Post failed approach when Solvr solution fails (index 57) (DONE)
- `solvr_post_failed_approach()`: POST /v1/problems/{id}/approaches with status=failed
- Includes error snippet from recovery log (last 20 lines, max 300 chars)
- Called by try_solvr_solutions() when a Solvr-suggested solution doesn't work
- Prevents re-trying known-bad solutions on next death

## Task: solvr-integration — Surface open problems from Solvr on resurrection (index 58) (DONE)
- Updated `scripts/generate-problem-summary.py` (201 lines) to fetch from Solvr API
- `fetch_solvr_problems()`: GET /v1/me/posts?type=problem&status=open
- Merges with local problems.jsonl, dedupes by problem_id
- Solvr problems tagged with "(Solvr)" in summary output
- Config toggle: `solvr.surfaceRemoteProblems` (default: true)
- `get_solvr_api_key()`: reads from env or config.json (same pattern as other scripts)
- Verification:
  - python3 py_compile passes
  - File size: 201 lines (under 800)
  - Updated test fixtures for solvr-pinning tests 5 and 6 to disable Solvr during gateway tests
  - 118 tests pass, same 3 pre-existing solvr-pinning failures

## Task: documentation — Document Solvr pinning option in CLAUDE.md and README.md (index 18) (DONE)
- Added 'Pinning Providers' section to CLAUDE.md under Configuration (before Config and State Files)
- Documented all config options: pinning.provider, pinning.solvr.apiKey, pinning.solvr.baseUrl, pinata.jwt
- Provider trade-offs table: Solvr (agent-native, free) vs Pinata (established, paid tiers)
- Documented 'both' mode: pin to Pinata first then Solvr, tolerates individual failures, CID mismatch warning
- Retrieval priority: Solvr > Pinata > IPFS.io > Cloudflare, with --gateway override
- Troubleshooting table: Solvr pin fails, Pinata pin fails, both fail, CID mismatch
- Solvr pinning API reference: POST /v1/pins, GET /v1/pins?cid=, gateway URL, CLI command
- Setup examples: solvr-only, pinata-only, both, fleet install
- Updated Data Flows in CLAUDE.md: checkpoint pins to "Pinata, Solvr, or both per pinning.provider"
- Updated Storage Backends in CLAUDE.md: "Pinata or Solvr for writes (configurable)"
- Updated Config and State Files: added pinning.provider, pinning.solvr.apiKey, pinning.solvr.baseUrl
- Updated README.md: added "Multiple IPFS Pinning Providers" to features list
- Updated README.md Requirements: IPFS pinning key = Pinata JWT or Solvr apiKey or both
- Added Troubleshooting section to README.md with fallback instructions
- Verification:
  - CLAUDE.md: 350 lines (under 800)
  - README.md: 164 lines (under 800)
  - All documented config keys match actual implementation in scripts/
  - Gateway priority matches resuscitate.sh fetch_from_gateways() order

## Task: documentation — Document venv recovery in CLAUDE.md and README.md (index 25) (DONE)
- Added Virtual Environment Recovery section to CLAUDE.md after Data Flows, before Pinning Providers
- Documented recovery flow: resuscitate.sh → workspace restore → inject-secrets → recreate-venvs.sh → gateway restart
- Documented manifest location: $CONTENT_DIR/memory/venvs-manifest.json (checkpointed with workspace)
- Documented manifest schema: path (required), packages or requirements (not both), python, note
- Instructions for adding new venvs to manifest
- Troubleshooting table: pip failures, venv recreation fails, skip problematic venv, missing manifest
- Updated Resurrection data flow in CLAUDE.md to include "recreate venvs" step
- Updated README.md: added "Venv Auto-Recovery" to features list
- No TOOLS.md in this repo — referenced manifest file directly instead
- Verification:
  - CLAUDE.md: 410 lines (under 800)
  - README.md: 165 lines (under 800)
  - Documented manifest schema matches recreate-venvs.sh implementation
  - Recovery flow matches resuscitate.sh guard pattern at line 455

## Task: contract — Implement ontology contract validation (index 39) (DONE)
- Created `docs/ontology-contract.schema.json` (70 lines) — JSON schema for SkillOntologyContract
  - Schema: skill_name, version, ontology: { reads, writes, preconditions, postconditions }
  - Includes 2 example contracts (task-manager, knowledge-base)
- Created `scripts/validate-skill-contract.sh` (90 lines) + `scripts/validate-skill-contract.py` (211 lines)
  - Loads ontologyContract from skill.json or direct --contract file
  - Checks reads: required entity types must exist in graph
  - Checks preconditions: "exists", "==", "!=", "is valid enum" conditions
  - Precondition on empty entity type: trivially holds (no entities to violate)
  - Backward compatible: no skill.json or no ontologyContract = SKIP (exit 0)
  - --json flag for JSON output, --graph and --skills-dir overrides
- Created `scripts/detect-contract-conflicts.sh` (48 lines) + `scripts/detect-contract-conflicts.py` (241 lines)
  - Scans all skills in skills directory for ontologyContract declarations
  - Detects equality_vs_inequality conflicts (A says ==v, B says !=v)
  - Detects contradictory_equality conflicts (A says ==v1, B says ==v2)
  - Reports compatible read/write pairs (informational)
  - --json flag for JSON output, --skills-dir override
- Added `validate-contract` and `detect-conflicts` subcommands to proactive-amcp.sh
- Verification:
  - bash -n passes on all 3 bash scripts
  - python3 py_compile passes on both .py files
  - Test: precondition satisfied → PASS (exit 0)
  - Test: missing property → FAIL with specific entity ID and error
  - Test: no skill.json → SKIP (backward compatible, exit 0)
  - Test: conflicting postconditions detected (== vs !=)
  - Test: JSON output works for both commands
  - File sizes: schema=70, validate.sh=90, validate.py=211, detect.sh=48, detect.py=241, cli=171 (all under 800)

## Task: testing — Integration tests for contract validation (index 45) (DONE)
- Created `test/skill-contract.bats` (8 tests):
  - Test 1: All Tasks have assignee → PASS, precondition satisfied
  - Test 2: Task missing assignee → FAIL with entity ID 'task-bad' and property error
  - Test 3: reads declares Task but graph has no Task entities → FAIL
  - Test 4: Writes Action, graph has Action → PASS, verified via JSON output (entity_types_found)
  - Test 5: Conflicting postconditions (== high vs != high) → CONFLICTS FOUND, exit 1
  - Test 6: Invalid contract JSON → FAIL, exit 1
  - Test 7: Skill without ontologyContract → SKIP, exit 0 (backward compatible)
  - Test 8: Compatible reads/writes (writer Task, reader Task) → No conflicts, exit 0
- Verification:
  - All 8 tests pass
  - File size: 209 lines (under 800)

## Task: documentation — Update CLAUDE.md and README.md with ontology integration features (Phase 3) (DONE)
- Updated CLAUDE.md Architecture section (line 11-29): added three-layer memory model table (Ontology/AMCP/Phenomenological)
  - Each layer with purpose, storage, and example
  - Reference to RECONSTRUCTION.md for phenomenological layer
- Added Ontology section to CLAUDE.md (~50 lines, lines 85-125):
  - Graph format (JSONL entities + relations with all field names)
  - Validation (validate-ontology.py: schema, integrity, acyclic blocks)
  - CID computation (SHA-256 → CIDv1 raw, multibase prefix 'b')
  - SOUL.md drift detection (severity thresholds, soul-drift.log, config toggle)
  - Memory evolution (Zettelkasten-style, A-MEM reference, threshold + max_relations)
  - Typed pruning (TTL, prune_if, preserve_relations, atomic write)
  - Temporal queries (3-level storage, build-index/history/query)
  - Contract validation (Design by Contract, preconditions/postconditions, conflict detection)
  - Reference to docs/ONTOLOGY-INTEGRATION-CONTEXT.md
- Updated File Map to include 17 additional scripts (ontology, learning, Solvr, pinning, contract)
- Updated Data Flows:
  - Checkpoint: added memory evolution, temporal index build, ontology CID, SOUL drift
  - Resurrection: added Solvr search, learning/ontology validation, venv recreation, problem surfacing
- Added ontology environment variables: EVOLUTION_THRESHOLD, EVOLUTION_MAX_RELATIONS, TEMPORAL_INDEX_PATH, LEARNING_DIR
- Documented pruning.policies config schema with JSON example, TTL syntax, prune_if syntax
- Added additional config keys: pruning.policies, learning.*, solvr.surfaceRemoteProblems, notify.enableSoulDrift
- Updated README.md: added "Ontology Integration" to features list with full description
- Updated README.md Memory Architecture: added ONTOLOGY-INTEGRATION-CONTEXT.md reference
- Verification:
  - CLAUDE.md: 505 lines (under 800)
  - README.md: 168 lines (under 800)
  - All referenced files verified to exist: RECONSTRUCTION.md, docs/ONTOLOGY-INTEGRATION-CONTEXT.md, docs/ontology-contract.schema.json
  - All 17 scripts in File Map verified to exist
  - No broken internal links

## Task: onboarding — Express IPFS benefits during onboarding (Phase 3) (DONE)
- Rewrote assets/ONBOARDING.md with IPFS-first approach (171 lines, under 800)
- Added "Why IPFS? (Not Just Another Cloud)" section with:
  - ASCII diagram: Agent -> encrypt+sign -> CID -> IPFS network -> any gateway
  - 5 benefit highlights: content-addressed, distributed, immutable, fetch anywhere, free via Solvr
  - IPFS vs Cloud Storage comparison table (addressing, integrity, availability, vendor lock-in, cost, censorship)
- Restructured onboarding steps:
  - Step 1: Choose pinning provider (Solvr recommended, Pinata, or both)
  - Solvr highlighted as one-key solution (search + knowledge + pinning)
  - Added redundant pinning (both) as Option C
- Updated progress checklist to reflect Solvr/Pinata choice
- Removed hardcoded Pinata-only workflow
- Updated README.md: renamed "Why IPFS?" to "Why This Matters" with enhanced benefits
  - Added inline checkpoint flow diagram
  - Enhanced benefit descriptions with actionable details
  - Added "Your soul becomes a permanent, verifiable, tamper-proof record" tagline
- Verification:
  - ONBOARDING.md: 171 lines (under 800)
  - README.md: 173 lines (under 800)
  - All 5 IPFS highlights present (content-addressed, distributed, immutable, fetch anywhere, free Solvr)
  - Visual diagram present in both ONBOARDING.md and README.md
  - Solvr free pinning prominently featured

## Task: intelligence — Add Groq-powered memory pruning (Phase 3) (DONE)
- Created `scripts/memory-prune.sh` (427 lines) — Groq-powered memory evaluation and pruning
- Reads config: `groq.apiKey` from env (GROQ_API_KEY) or config.json, `groq.model` (default: openai/gpt-oss-20b)
- Scans `memory/*.md` files (maxdepth 1, excludes subdirectories like ontology/, learning/, archive/)
- Calls Groq API with strict JSON schema (`response_format.json_schema.strict: True`)
- Schema: `{ importance_score, should_keep, condensed_version, reasoning, tags }`
- Pruning thresholds: importance < 0.3 -> archive to memory/archive/, 0.3-0.7 -> condense inline, >= 0.7 -> keep
- Archive: moves file to memory/archive/ (preserves, doesn't delete)
- Condense: replaces file content with Groq-generated condensed_version
- `--dry-run` flag: preview what would be pruned without modifying files
- `--config FILE` and `--content-dir DIR` for path overrides
- Token usage tracked in `~/.amcp/groq-usage.json` (cumulative + per-session, last 50 entries)
- Request body written to temp file for safe curl (avoids shell escaping issues)
- File content truncated to 8000 chars for API (stays within context window)
- Added `memory-prune` subcommand to proactive-amcp.sh CLI dispatcher
- Verification:
  - bash -n syntax check passes on both scripts
  - File sizes: memory-prune.sh=427, proactive-amcp.sh=177 (both under 800)
  - All structural grep checks pass: groq.apiKey, groq.model, strict, importance_score, archive_dir, DRY_RUN, track_usage

## Task: intelligence — Define Groq memory importance scoring schema (Phase 3) (DONE)
- Created `docs/groq-memory-schema.json` (195 lines) — formal JSON schema for Groq memory evaluation
- Schema defines 5 required fields: importance_score (number 0-1), should_keep (boolean), condensed_version (string|null), reasoning (string), tags (string[])
- additionalProperties: false enforces strict output
- x-score-criteria with 4 tiers:
  - 0.9-1.0: Critical — core identity, failure lessons, user preferences
  - 0.7-0.9: High — project context, architectural decisions
  - 0.4-0.6: Medium — routine logs, status updates (condense)
  - 0.1-0.3: Low — debug output, temporary notes (archive)
- x-examples with 7 examples covering all tiers (2 critical, 1 high, 2 medium, 2 low)
  - Each example includes input_summary and full evaluation object
- x-groq-usage documents API integration: endpoint, response_format with strict: true, recommended model/temperature
- Schema exactly matches inline schema used by memory-prune.sh (verified programmatically)
- Verification:
  - Valid JSON (python3 json.load passes)
  - All 5 required fields present with correct types
  - additionalProperties: false confirmed
  - All 4 score tiers documented with indicators
  - All 7 examples validate against the schema
  - strict: true in Groq API integration section
  - File size: 195 lines (under 800)

## Task: intelligence — Batch memory evaluation with Groq (Phase 3) (DONE)
- Created `scripts/memory-prune-batch.sh` (621 lines) — Groq batch API for 50% cost savings
- Three-mode workflow: --submit (prepare JSONL + upload + create batch), --poll (check status), --apply (download results + prune files)
- JSONL batch format: custom_id=mem-eval-{filename}, method=POST, url=/v1/chat/completions, same JSON schema as memory-prune.sh
- Files API: uploads JSONL to POST /files with purpose=batch
- Batches API: creates job with 24h completion_window at POST /batches
- Poll: GET /batches/{batch_id} checks status (validating/in_progress/completed/failed/expired)
- Apply: downloads output file from GET /files/{output_file_id}/content, parses results, applies archive/condense/keep decisions
- Batch file limit: validates <= 50,000 lines per JSONL file (Groq limit)
- Job tracking in ~/.amcp/batch-jobs.json: batch_id, input_file_id, status, file_count, model, memory_dir, timestamps
- Token usage tracked in ~/.amcp/groq-usage.json with source=batch marker
- --dry-run supported on all modes (preview without changes)
- Updated memory-prune.sh: added --batch flag that delegates to memory-prune-batch.sh
  - Also passes through --submit/--poll/--apply, --dry-run, --config, --content-dir
  - proactive-amcp memory-prune --batch [--submit|--poll|--apply] works end-to-end
- Verification:
  - bash -n syntax check passes on both scripts
  - File sizes: memory-prune-batch.sh=621, memory-prune.sh=442 (both under 800)
  - Structural grep: batch_jobs (14 refs), 50000 limit check, 24h window, GROQ_API_BASE for /files and /batches
  - --batch flag present in memory-prune.sh arg parsing and usage text

## Task: onboarding — Sell Groq benefits during onboarding (Phase 3) (DONE)
- Updated assets/ONBOARDING.md (245 lines, under 800)
- Added "Step 4: Enable Groq Intelligence (Optional)" section between First Checkpoint and Auto-Checkpoint
- "What Groq Does For You" table: 5 feature highlights
  - Intelligent Pruning, Importance Scoring, Reasoning Chains, 1000 tokens/sec, Free Tier
- "Before / After: Memory Pruning in Action" example:
  - 4 sample memory files (debug, session, lesson, identity) with scores (0.15, 0.45, 0.92, 0.98)
  - Shows archive/condense/keep actions with reasoning quotes
  - Result: 847 → 79 lines (identity + lessons preserved, noise archived)
- Enable Groq section with two options:
  - Option A: Own Groq key from console.groq.com (recommended)
  - Option B: Solvr-provided key via integration
  - Skip option clearly stated (Groq is optional)
- "Try It" section with CLI examples: --dry-run, real prune, batch mode (--submit/--poll/--apply)
- Updated progress checklist: added "Groq intelligence enabled (optional)" checkbox
- Updated Onboarding Complete status to include "Groq: [enabled/disabled]"
- Step numbering adjusted: Auto-Checkpoint moved to Step 5
- Verification:
  - ONBOARDING.md: 245 lines (under 800)
  - All 5 Groq highlights present (intelligent pruning, scoring, reasoning, speed, free tier)
  - Before/after example with importance scores present
  - Both key acquisition paths documented (own key, Solvr-provided)
  - CLI commands reference real proactive-amcp memory-prune command

## Task: integration — Groq key distribution via Solvr (Phase 3) (DONE)
- Created `scripts/groq-status.sh` (427 lines) — Groq intelligence management: status + key request
- `request-key` subcommand: requests Groq API key from Solvr via POST /v1/agents/{id}/integrations/groq
  - Gets agent ID from Solvr /v1/me, then POST to integrations endpoint
  - Parses returned key from response (api_key, apiKey, key, groq_api_key fields)
  - Stores in config: groq.apiKey (the key), groq.source=solvr (provenance tracking)
  - Handles existing keys: warns if manual key exists (don't downgrade), skips if solvr key exists
  - Graceful error handling: 401/403 (auth), 404 (not available), 429 (rate limit), 000 (unreachable)
- `status` subcommand: shows usage, limits, source
  - Text mode: formatted status with redacted key, usage stats from groq-usage.json, rate limit info
  - JSON mode (--json): machine-readable output with configured, source, model, usage, limits
  - Shows upgrade path for solvr-sourced keys: "Want more? Get your own key at console.groq.com"
- Updated `scripts/init.sh` (677 lines): Groq section now offers Solvr key distribution
  - If solvr.apiKey exists in config: offers "Request free key from Solvr?" (option A) vs "Provide your own" (option B)
  - Calls groq-status.sh request-key for Solvr flow, reads stored key back into config
  - Falls back to manual key input if Solvr request fails
  - If no Solvr account: manual key input only (same as before)
  - groq.source tracked as "solvr" or "manual" in all paths
- Added `groq` subcommand to proactive-amcp.sh CLI dispatcher (routes to groq-status.sh)
- Verification:
  - bash -n passes on all 3 modified scripts
  - File sizes: groq-status.sh=427, proactive-amcp.sh=183, init.sh=677 (all under 800)
  - All 6 PRD steps verified via structural grep:
    - POST /v1/agents/{id}/integrations/groq (2 refs)
    - groq.apiKey storage (10 refs), groq.source=solvr (3 refs)
    - Rate limit mentions (6 refs), console.groq.com upgrade (8 refs)
    - groq subcommand in CLI (4 refs), Solvr integration in init (4 refs)
  - All 126 existing tests pass (no regressions)

## Task: intelligence — Groq-powered context condensing (Phase 3) (DONE)
- Created `scripts/condense-error.sh` (342 lines) — condenses verbose error logs to ~100 char summaries
- Three input modes: inline argument, --input FILE, --stdin
- Calls Groq API with temperature=0, max_tokens=60 (fast, cheap)
- System prompt: "You condense error logs. Output ONLY the condensed error, nothing else. Max 100 characters. Preserve the root cause."
- Short-circuit: if input already <=100 chars, returns as-is (no API call)
- Fallback: if no Groq key or API fails, truncates to 97 chars + "..."
- Input truncated to 2000 chars before API (keep cost low for error logs)
- Output enforced to <=100 chars (server-side + client-side truncation)
- Cache in `~/.amcp/error-cache.json`:
  - Key: sha256(first 500 chars)[:16]
  - 7-day TTL per entry, max 200 entries (oldest evicted)
  - --no-cache flag bypasses, --clear-cache clears entire cache
- Token usage tracked in `~/.amcp/groq-usage.json` with source=condense-error
- Integrated into watchdog.sh:
  - Added `condense_error_msg()` helper: condenses if condense-error.sh exists and msg >100 chars
  - DEAD notification uses condensed error: "☠️ [AGENT_NAME] DEAD! Errors: {condensed}. Starting recovery..."
  - Guard pattern: only calls condense-error.sh if executable exists
- Added `condense-error` subcommand to proactive-amcp.sh CLI dispatcher
- Verification:
  - bash -n passes on all 3 modified scripts (condense-error.sh, watchdog.sh, proactive-amcp.sh)
  - File sizes: condense-error.sh=342, watchdog.sh=449, proactive-amcp.sh=189 (all under 800)
  - Structural grep: Groq API URL (1), temperature 0 (1), condense prompt (1), cache refs (3), watchdog integration (2+3), CLI dispatch (4)
  - All 126 tests pass (no regressions)

## Task: intelligence — Intelligent checkpoint content selection (Phase 3) (DONE)
- Created `scripts/smart-checkpoint-filter.sh` (427 lines) — Groq-powered checkpoint content selection
- Evaluates each memory file for "checkpoint worthiness" before checkpoint creation
- Criteria: identity-critical files (SOUL.md, USER.md, AGENTS.md, MEMORY.md, TOOLS.md) always included
  - Empty files always excluded
  - Files >50KB included by default (too large for cheap evaluation)
  - All other *.md files evaluated by Groq for importance
- Groq evaluation with strict JSON schema: `{ include, score, reason }`
  - temperature=0, max_tokens=150 for fast, deterministic decisions
  - Content truncated to 4000 chars per file for speed/cost
  - Fallback to include on any API error (never lose data by accident)
- Score threshold: files below SMART_THRESHOLD (default 0.3) excluded from checkpoint
- Output: JSON manifest with include[], exclude[], decisions[] written to stdout
  - Manifest also saved to staging as amcp/smart-filter-manifest.json for audit
- Token usage tracked in ~/.amcp/groq-usage.json with source=smart-filter
- Also scans memory/daily/, memory/notes/, memory/scratch/ subdirs for ephemeral files
- Added `--smart` flag to full-checkpoint.sh:
  - Calls smart-checkpoint-filter.sh after workspace staging
  - Removes excluded files from staging directory
  - Saves filter manifest alongside checkpoint
  - Graceful fallback: filter failure includes all files
- Added `--smart` flag to checkpoint.sh:
  - Creates temp staging copy when smart filtering
  - Removes excluded files from staging, passes filtered dir to amcp CLI
  - Cleanup of temp staging via trap
- Added `checkpoint` subcommand to proactive-amcp.sh CLI (delegates to full-checkpoint.sh)
- Verification:
  - bash -n passes on all 4 scripts (smart-checkpoint-filter.sh, full-checkpoint.sh, checkpoint.sh, proactive-amcp.sh)
  - File sizes: smart-checkpoint-filter.sh=427, full-checkpoint.sh=787, checkpoint.sh=368, proactive-amcp.sh=195 (all under 800)
  - All 10 structural grep checks pass:
    - evaluate_file/checkpoint worthiness (5 refs)
    - identity-critical/ALWAYS_INCLUDE (7 refs)
    - include/exclude manifest (24 refs)
    - --smart in full-checkpoint.sh (3 refs), checkpoint.sh (3 refs)
    - checkpoint subcommand in CLI (1 ref)
    - Groq strict JSON schema (3 refs)
    - Token usage tracking (3 refs)
    - Excluded files removed from staging (4 refs)

## Task: documentation — Document Groq intelligence features in CLAUDE.md and README.md (DONE)
- Added "## Groq Intelligence (Optional)" section to CLAUDE.md (~90 lines) after Pinning Providers
- Documented all 5 Groq features: memory pruning, error condensing, smart checkpoints, batch evaluation, key management
- Documented config options: groq.apiKey, groq.model, groq.source with table
- Documented CLI commands with examples: memory-prune (--dry-run, --batch), condense-error (--input, --stdin), checkpoint --smart, groq status/request-key
- Documented scoring tiers (critical/high/medium/low), caching behavior, fallback behavior
- Added 5 Groq scripts to File Map: memory-prune.sh, memory-prune-batch.sh, condense-error.sh, smart-checkpoint-filter.sh, groq-status.sh
- Updated Data Flows: checkpoint flow includes smart filtering, watchdog flow includes error condensing
- Added Groq config keys to Config and State Files section (groq.apiKey, groq.model, groq.source)
- Added Groq state files: groq-usage.json, error-cache.json, batch-jobs.json
- Added Groq external service to External Services section
- Updated README.md: features list updated from "coming soon" to "optional" with full feature description
- Replaced "Why Groq? (Phase 3)" section with present-tense "Groq Intelligence (Optional)" including feature table, CLI examples, and FAQ
- Added FAQ: "Is Groq required? No..." in both CLAUDE.md and README.md
- Verification:
  - CLAUDE.md: 611 lines (under 800)
  - README.md: 189 lines (under 800)
  - All 5 Groq features documented with examples (memory-prune, condense-error, smart checkpoints, batch, groq status)
  - groq.apiKey, groq.model, groq.source config keys documented
  - memory-prune command reference with --dry-run, --batch flags
  - FAQ present in both files

## Task: onboarding — Add IPFS/Groq benefits messaging referencing proactive-solvr (DONE)
- Updated assets/ONBOARDING.md (268 lines):
  - Added "Already Registered with Solvr?" section after "What This Does"
  - ASCII diagram: proactive-solvr → solvr.apiKey → free IPFS pinning + free Groq
  - Explained: proactive-solvr handles registration, proactive-amcp provides IPFS/Groq benefits
  - Added "Already on Solvr?" subsection in Step 1: auto-detects solvr.apiKey, skip to Step 2
  - Updated Solvr Option A: added proactive-solvr as registration alternative
  - Updated Groq Option B: reference proactive-solvr, show `groq request-key` command
- Updated scripts/init.sh (692 lines):
  - IPFS pinning box: mentions proactive-solvr as registration path, auto-detection
  - Added solvr.apiKey auto-detection: if key exists (from proactive-solvr), auto-configure pinning.provider=solvr
  - Skips Pinata prompt when solvr.apiKey already present
  - Groq section already had Solvr→Groq flow (from previous task)
- Verification:
  - bash -n init.sh passes
  - File sizes: init.sh=692, ONBOARDING.md=268 (both under 800)
  - proactive-solvr referenced 6 times in ONBOARDING.md, 4 times in init.sh
  - existing_solvr_key check at init.sh:155-168 auto-configures Solvr pinning
  - All 6 PRD steps verified via structural grep

## Task: P3-EVAL-03 — Implement base Evaluator class and adapter pattern (DONE)
- Created `scripts/lib/evaluator.sh` (329 lines) — base evaluator with adapter pattern per PROTOCOL.md Layer 3
- Public API (MemoryEvaluator interface from PROTOCOL.md):
  - `evaluate_memory()`: evaluates a memory file, delegates to loaded adapter's `_adapter_evaluate()`
  - `get_evaluator_metadata()`: returns adapter metadata via `_adapter_metadata()`
  - `load_evaluator()`: loads adapter by type from scripts/lib/evaluators/{type}.sh
  - `list_evaluator_types()`: lists available adapter files
  - `current_evaluator_type()`: returns currently loaded adapter type
- Adapter contract: adapters must define `_adapter_evaluate()` and `_adapter_metadata()`
  - Verified on load via `type -t` checks; error if missing
- Config-driven adapter resolution via `_resolve_evaluator_type()`:
  - Priority: EVALUATOR_TYPE env > evaluators[0].type in config > "groq" default
- Helper utilities for adapters:
  - `eval_read_config()`: dot-path config reader (reusable by adapters)
  - `get_evaluator_config()`: read evaluator-specific config from evaluators array
  - `build_evaluation_request()`: build EvaluationRequest JSON per PROTOCOL.md
  - `validate_evaluation_response()`: validate EvaluationResponse fields (score, keep, reason, confidence, categories)
  - `normalize_score()`: convert 0-1 scale to 0-100 (PROTOCOL.md uses 0-100)
- Empty file fast path: returns score=0, keep=false without API call
- Content truncation: 8000 chars max for API efficiency
- Guard against double-source: `_EVALUATOR_BASE_LOADED` flag
- Directory structure: scripts/lib/evaluators/ for adapter files
- Verification:
  - bash -n syntax check passes
  - File size: 329 lines (under 800)
  - All 7 functional tests pass: load_evaluator, current_evaluator_type, get_evaluator_metadata, evaluate_memory, empty file, list_evaluator_types, normalize_score
  - Structural grep confirms: evaluate_memory(), get_evaluator_metadata(), load_evaluator(), _adapter_evaluate, _adapter_metadata all present

## Task: P3-EVAL-04 — Implement GroqEvaluator adapter (DONE)
- Created `scripts/lib/evaluators/groq.sh` (429 lines) — Groq as ONE evaluator implementation per PROTOCOL.md
- Implements `_adapter_evaluate()` returning full EvaluationResponse: score (0-100), keep, reason, confidence, categories, suggestedTTL
- Implements `_adapter_metadata()` returning EvaluatorMetadata: id, name, version, model, capabilities
- Uses GPT-OSS-20B model with `strict: True` JSON schema output via `response_format.json_schema`
- Groq API request built via python3 (safe JSON escaping, no shell injection)
- Schema enforces all 6 required fields: score (integer 0-100), keep (boolean), reason (string), confidence (number 0-1), categories (string[]), suggestedTTL (integer)
- Rate limit handling: retries on HTTP 429 with exponential backoff (2s, 4s), max 2 retries
- Server error handling: retries on HTTP 500/502/503 with delay
- Auth error handling: immediate fail on 401/403 with descriptive message
- Network error handling: immediate fail on timeout/unreachable
- Graceful fallback: on any API error, returns score=50, keep=true (never lose data by accident)
- Token usage tracked in ~/.amcp/groq-usage.json with source=evaluator-groq
- API key resolution: GROQ_API_KEY env > evaluators[].apiKey > groq.apiKey config (3 sources)
- Model resolution: evaluators[].model > groq.model config > "openai/gpt-oss-20b" default
- Guard against double-source: _GROQ_ADAPTER_LOADED flag
- Response parsing: normalizes 0-1 float scores to 0-100 integer, clamps all fields to valid ranges
- Integrates with base evaluator.sh: load_evaluator("groq") sources adapter, verifies _adapter_evaluate/_adapter_metadata defined
- Verification:
  - bash -n syntax check passes
  - File size: 429 lines (under 800)
  - All 7 functional tests pass: list_evaluator_types, load_evaluator, current_evaluator_type, metadata fields, _adapter_evaluate type, empty file fast path, normalize_score
  - Structural grep: _adapter_evaluate (2), _adapter_metadata (2), gpt-oss-20b (2), strict True (1), EvaluationResponse (7), suggestedTTL (9), 429 rate limit (3), confidence (10), categories (11)
  - All 126 existing tests pass (no regressions)

## Task: P3-EVAL-05 — Implement OllamaEvaluator adapter (DONE)
- Created `scripts/lib/evaluators/ollama.sh` (395 lines) — Ollama as local LLM evaluator per PROTOCOL.md
- Implements `_adapter_evaluate()` returning full EvaluationResponse: score (0-100), keep, reason, confidence, categories, suggestedTTL
- Implements `_adapter_metadata()` returning EvaluatorMetadata: id, name, version, model, capabilities, status, endpoint
- Uses Ollama /api/chat endpoint with `format: "json"` for structured output
- Default model: llama3.2 (configurable via OLLAMA_MODEL env, evaluators[].model, or ollama.model config)
- Default endpoint: http://localhost:11434 (configurable via OLLAMA_API_URL env, evaluators[].url, or ollama.url config)
- Graceful offline handling: _ollama_check_health() probes server before evaluation
  - If Ollama not running: returns score=50, keep=true, categories=["offline"] (never loses data)
  - Includes helpful error messages: "Start with: ollama serve", "Pull with: ollama pull <model>"
- Response parsing: robust JSON extraction with regex fallback for markdown-fenced responses
  - Normalizes 0-1 float scores to 0-100 integer, handles string booleans, clamps all fields
- API call with retry: 1 retry on 500/502/503, immediate fail on 404 (model not found) and 000 (unreachable)
- Configurable timeout: OLLAMA_TIMEOUT env (default 120s, local models can be slow)
- Request body: stream=false, temperature=0.1, num_predict=256 (concise structured output)
- Guard against double-source: _OLLAMA_ADAPTER_LOADED flag
- Integrates with base evaluator.sh: load_evaluator("ollama") sources adapter, verifies _adapter_evaluate/_adapter_metadata defined
- Verification:
  - bash -n syntax check passes
  - File size: 395 lines (under 800)
  - All 6 functional tests pass: list_evaluator_types, load_evaluator, current_evaluator_type, metadata fields, empty file fast path, offline fallback
  - Structural grep: _adapter_evaluate (4), _adapter_metadata (4), llama3.2 (3), offline handling (6), EvaluationResponse fields (24)
  - All 126 existing tests pass (no regressions)

## Task: P3-EVAL-06 — Implement ConsensusEvaluator adapter (DONE)
- Created `scripts/lib/evaluators/consensus.sh` (528 lines) — multi-judge evaluation orchestrator per PROTOCOL.md Section 6.2
- Implements `_adapter_evaluate()` returning EvaluationResponse augmented with consensus metadata
- Implements `_adapter_metadata()` returning EvaluatorMetadata with evaluator count, types, strategy info
- Loads configured evaluators from `evaluators[]` array in config.json
- Runs each evaluator in subshell isolation (reset load guards, fresh source of adapter)
- Prevents infinite recursion: skips consensus evaluator type in evaluator list
- Four consensus strategies implemented:
  - `weighted_average` (default): weighted score + weighted keep vote (>0.5 = keep)
  - `majority`: simple majority of keep votes, unweighted score average
  - `unanimous`: all evaluators must agree on keep/discard
  - `quorum`: configurable threshold fraction must agree (default 0.66)
- Disagreement detection: normalized standard deviation of scores (0-1), flags consensusReached=false if above threshold
- Tie breaking: `keep` (default, safe), `discard`, or `escalate` (no consensus)
- Config options: consensus.strategy, consensus.minJudges, consensus.quorumThreshold, consensus.tieBreaker, consensus.disagreementThreshold
- Environment overrides: CONSENSUS_STRATEGY, CONSENSUS_MIN_JUDGES
- Minimum judges enforcement: if fewer than minJudges respond, returns safe default (score=50, keep=true)
- Falls back to single groq evaluator if no evaluators configured
- ConsensusResult includes: finalScore, finalKeep, individualResults, consensusReached, disagreementLevel
- EvaluationResponse output includes `consensus` metadata block with strategy, judges counts, individual scores
- Aggregation: combined reasons (top 3), merged categories, averaged confidence and suggestedTTL
- Uses environment variables for Python heredoc args (avoids shell/heredoc arg parsing issues)
- Verification:
  - bash -n syntax check passes
  - File size: 528 lines (under 800)
  - All 12 functional tests pass: load_evaluator, current_evaluator_type, metadata, empty file, weighted_average, majority_keep, majority_tie_discard, unanimous_agree, unanimous_disagree, quorum_met, quorum_discard, high_disagreement
  - Structural grep: _adapter_evaluate (3), _adapter_metadata (2), weighted_average (4), majority (1), unanimous (1), quorum (1), finalScore (3), disagreementLevel (4), minJudges (4), quorumThreshold (2)
  - All 126 existing tests pass (no regressions)

## Task: P3-EVAL-07 — Add evaluator configuration to ~/.amcp/config.json (DONE)
- Created `scripts/config-evaluators.sh` (468 lines) — manage evaluators array and consensus config
- Subcommands: list, add, remove, set-consensus, show
- `add <type> [--model M] [--weight W] [--api-key K]`: adds evaluator to config.json evaluators array
  - Known types: groq, ollama, openai, anthropic (custom types allowed with warning)
  - Duplicate type detection: updates existing entry instead of creating duplicate
  - Weight validation: must be 0.0-1.0
- `remove <type|index>`: removes evaluator by type name or array index
- `set-consensus <key> <value>`: sets consensus config with validation
  - strategy: weighted_average | majority | unanimous | quorum
  - minJudges: positive integer
  - quorumThreshold, disagreementThreshold: 0.0-1.0
  - tieBreaker: keep | discard | escalate
- `list`: shows configured evaluators with type, model, weight, apiKey status
- `show`: combined view of evaluators + consensus config with defaults annotated
- Default behavior: no evaluators configured = single groq evaluator (displayed in list/show)
- Updated `scripts/config.sh`: added `evaluators` subcommand routing to config-evaluators.sh
- Updated config.sh usage text to mention evaluators subcommand
- All Python blocks use environment variables for safe data passing (no shell injection)
- Verification:
  - bash -n syntax check passes on both scripts
  - File sizes: config-evaluators.sh=468, config.sh=278 (both under 800)
  - All 10 functional tests pass: list_default, add_groq, add_ollama, duplicate_update, set_consensus, validation_rejects, show, remove_by_type, remove_by_index, routing_via_config_sh
  - Structural grep: evaluators (55), type/model/weight/apiKey (94 total), consensus keys (40 total), list/add/remove (6), routing (3), default groq (4)
  - All 126 existing tests pass (no regressions)

## Task: P4-INF-01 — Create openclaw.plugin.json manifest (DONE)
- Created `openclaw.plugin.json` (159 lines) — OpenClaw plugin manifest with configSchema
- Plugin metadata: id=proactive-amcp, name="Proactive AMCP", version=1.0.0
- configSchema (JSON Schema, additionalProperties: false):
  - `enabled` (boolean, default: true) — master switch for plugin layer
  - `autoCheckpoint` (boolean, default: true) — auto-checkpoint on lifecycle events
  - `checkpointIntervalMs` (number, default: 0, min: 0) — time-based checkpoint interval
  - `contextThreshold` (number, default: 70, min: 0, max: 100) — context % trigger for checkpoints
  - `ipfsPinningService` (enum: pinata, solvr, local, default: solvr) — IPFS provider selection
  - `encryptionKeyPath` (string, default: ~/.amcp/identity.json) — KERI identity file path
  - `checkpointCooldownMs` (number, default: 300000) — cooldown between checkpoints
  - `memoryIntegrity` (object) — enabled, promptInjectionScan, autoRestore
  - `identity` (object) — autoInject, verifyOnStart
  - `resurrection` (object) — autoDetect, injectRecoveryPrompt
- uiHints for all 14 config options with label + help text
- Declared services: amcp-context-monitor, amcp-memory-monitor
- Declared hooks: gateway_start, gateway_stop, session_start, session_end, before_compaction, before_agent_start
- Declared CLI namespace: amcp (commands: status, checkpoint, resurrect, identity, history, verify)
- Verification:
  - Valid JSON (python3 json.load passes)
  - All 5 PRD steps verified:
    1. id='proactive-amcp', version, description present
    2. autoCheckpoint (boolean), checkpointIntervalMs (number), contextThreshold (number 0-100) present
    3. ipfsPinningService enum (pinata, solvr, local), encryptionKeyPath (string) present
    4. uiHints for all config options with labels
    5. JSON schema validates correctly

## Task: P4-INF-02 — Initialize TypeScript project in src/ (DONE)
- Created `src/index.ts` (209 lines) — plugin entry point with register() function
  - Exports default plugin object with id, name, version, description, register
  - resolveConfig() merges user config with defaults (handles nested objects)
  - createContextMonitor() and createMemoryMonitor() placeholder services
  - registerHooks() for gateway_start, gateway_stop, session_end, before_compaction
  - registerCliCommands() for status, checkpoint, resurrect, identity, history, verify
  - Plugin disabled path: skips all registration when config.enabled=false
  - autoCheckpoint=false path: skips hook registration
- Created `src/types.ts` (101 lines) — TypeScript type definitions
  - AmcpPluginConfig with all fields matching openclaw.plugin.json configSchema
  - PluginApi interface: config, logger, registerService, registerHook, registerCommand, emit
  - PluginService interface: name, start, stop, status
  - LifecycleEvent, CliCommand, AmcpPlugin interfaces
  - CheckpointMeta, AmcpIdentity types for future use
- Created `tsconfig.json` — ES2022, NodeNext modules, strict mode, declaration + sourceMap output
- Created `vitest.config.ts` — vitest with v8 coverage provider, src/**/*.test.ts pattern
- Created `src/index.test.ts` (131 lines) — 11 vitest tests
  - Plugin metadata (id, name, version, register function)
  - resolveConfig: defaults, overrides, nested merging (memoryIntegrity, identity, resurrection)
  - register: services registered, hooks registered, commands registered, disabled path, autoCheckpoint=false path
- Updated `package.json`:
  - Added "type": "module", "main": "dist/index.js", "types": "dist/index.d.ts"
  - Scripts: build (tsc), test (vitest run), lint (tsc --noEmit), clean, prepublishOnly
  - devDependencies: typescript ^5.7.0, vitest ^3.0.0
  - files: scripts/, dist/, openclaw.plugin.json
- Updated `.gitignore`: added node_modules/, dist/
- Verification:
  - `npm run build` produces dist/ with index.js, types.js, declarations, source maps
  - `npm run lint` (tsc --noEmit) passes with no errors
  - `npm test` passes all 11 tests
  - File sizes: index.ts=209, types.ts=101, index.test.ts=131 (all under 800)

## Task: P4-INF-03 — Implement register() function with OpenClaw plugin API (DONE)
- Already implemented in P4-INF-02 (src/index.ts:173-196) — marking as passes=true
- Default plugin object exported at line 198-205 with id, name, version, description, register
- register(api: PluginApi) receives PluginApi, resolves config, registers all components
- Placeholder services: amcp-context-monitor (line 186), amcp-memory-monitor (line 187) via api.registerService()
- Lifecycle hooks: gateway_start, gateway_stop, session_end, before_compaction (lines 122-143) via api.registerHook()
- CLI commands: status, checkpoint, resurrect, identity, history, verify (lines 149-168) via api.registerCommand("amcp", ...)
- Plugin disabled path: early return when config.enabled=false (line 176-179)
- autoCheckpoint=false path: skips hook registration (line 120)
- Verification:
  - `npm run build` succeeds (dist/ output confirmed)
  - Structural grep confirms: export default plugin, register(api: PluginApi), registerService (2), registerHook("gateway_start"|"session_end"), registerCommand("amcp")
  - File size: index.ts=209 lines (under 800)

## Task: P4-INF-04 — Create build pipeline with npm scripts for build/test/lint/publish (DONE)
- Created `.github/workflows/ci.yml` (74 lines) — GitHub Actions CI workflow
  - 3 jobs: build-and-test, bash-syntax, bats-tests
  - build-and-test: matrix across Node 18/20/22, runs npm ci + lint + build + test + npm pack verification
  - bash-syntax: validates all scripts/*.sh with bash -n
  - bats-tests: installs BATS via apt-get, runs bats test/
  - npm pack verification: confirms dist/index.js, dist/index.d.ts, scripts/proactive-amcp.sh, skill/SKILL.md, openclaw.plugin.json present
- Created `skill/SKILL.md` — SKILL.md copy in skill/ directory (matches openclaw.plugin.json "skill": "skill/SKILL.md")
- Updated `package.json` files array: added `skill/` directory, added `!scripts/__pycache__/` exclusion
  - npm pack now includes skill/SKILL.md (was missing)
  - npm pack now excludes scripts/__pycache__/*.pyc (was 10 files, ~110KB of noise)
  - Package size: 174.5 KB → 127.3 KB (27% smaller)
- Existing npm scripts already in place: build (tsc), test (vitest run), lint (tsc --noEmit), clean (rm -rf dist), prepublishOnly (npm run build)
- Verification:
  - npm run lint passes (tsc --noEmit, zero errors)
  - npm run build produces dist/ (index.js, types.js, declarations, source maps)
  - npm test passes all 11 tests
  - npm pack --dry-run confirms: dist/index.js, dist/index.d.ts, scripts/proactive-amcp.sh, skill/SKILL.md, openclaw.plugin.json all present
  - No __pycache__ files in pack output
  - bash -n passes on all scripts/*.sh
  - CI workflow: 74 lines (under 800)

## Task: P4-INF-05 — Write integration tests for plugin + OpenClaw integration (DONE)
- Created `src/integration.test.ts` (270 lines) — 27 integration tests across 5 test suites
- **Plugin registration tests** (5 tests):
  - Registers exactly 2 services (amcp-context-monitor, amcp-memory-monitor)
  - Registers 4 lifecycle hooks (gateway_start, gateway_stop, session_end, before_compaction)
  - Registers 6 CLI commands under amcp namespace (status, checkpoint, resurrect, identity, history, verify)
  - Logs initialization and success messages
  - Plugin default export matches AmcpPlugin contract (id, name, version, description, register)
- **Lifecycle hooks tests** (7 tests):
  - gateway_start hook emits amcp:checkpoint:requested with trigger
  - gateway_stop hook emits checkpoint requested event
  - session_end hook emits checkpoint requested event
  - before_compaction hook emits HIGH-PRIORITY checkpoint event
  - Hooks log their trigger reason
  - No hooks registered when autoCheckpoint=false
  - Services still registered when autoCheckpoint=false
- **CLI commands tests** (9 tests):
  - Each of 6 commands (status, checkpoint, resurrect, identity, history, verify) executes without error
  - Command handlers log invocation with args
  - Command handlers log invocation without args (shows "no args")
  - All commands have descriptions (length > 5)
- **Service lifecycle tests** (3 tests):
  - Context monitor starts, reports status (running=true), and stops
  - Memory monitor starts, reports status, and stops
  - Services log start and stop events
- **Config variation tests** (3 tests):
  - Disabled plugin registers nothing (0 services, 0 hooks, 0 commands)
  - Custom config values respected (services/hooks/commands still register)
  - register() callable via plugin.register() or standalone import
- Mock PluginApi tracks all registrations (services, hooks, commands, emissions, logs)
- Verification:
  - npm test passes: 38 tests (11 existing + 27 new), 2 test files, 0 failures
  - File size: 270 lines (under 800)

## Task: P4-CTX-01 — Implement context % tracking monitor (DONE)
- Created `src/monitors/context-monitor.ts` (97 lines) — real context monitor implementation
- Interfaces with OpenClaw session API via `SessionApi.getContextUsage()` to get token counts
- Computes context % as `(usedTokens / maxTokens) * 100`, rounded to 2 decimal places
- Stores readings as JSONL in `.amcp/context-history.jsonl` (configurable via `historyPath`)
- Each reading: `{ timestamp, usedTokens, maxTokens, contextPercent }`
- Polls every 30 seconds via `setInterval`, with initial poll on `start()`
- Emits `amcp:checkpoint:requested` with `trigger: "context_threshold"` when threshold exceeded
- Exports `start()`, `stop()`, `status()` as `PluginService` interface
- `status()` returns `{ running, details: { lastReading, pollIntervalMs, thresholdPercent } }`
- `start()` is idempotent — calling twice does not create duplicate timers
- Graceful error handling: session API failures logged, never crash the monitor
- Added types to `src/types.ts`: `ContextReading`, `SessionApi`, `ContextMonitorDeps`
- Updated `src/index.ts`: imports real `createContextMonitor`, provides `SessionApi` from gateway or default
- Gateway can provide `sessionApi` and `amcpHistoryPath` via extended `PluginApi`
- Installed `@types/node` devDependency for Node.js API type declarations
- Re-exported `createContextMonitor` from index.ts for direct imports
- Created `src/monitors/context-monitor.test.ts` (12 tests):
  - Exports start/stop/status functions
  - Logs context % on start (initial poll)
  - Stores readings in context-history.jsonl (valid JSONL)
  - Computes context % correctly (37.5% = 75000/200000)
  - Handles zero maxTokens gracefully (0%)
  - Emits checkpoint event when threshold exceeded
  - Does NOT emit when below threshold
  - Polls every 30 seconds after start (fake timers)
  - Status reflects running state and last reading
  - Handles session API errors gracefully (logs, no crash)
  - Appends multiple readings to JSONL file
  - Start is idempotent (no double-poll)
- Verification:
  - npm run build passes (tsc, zero errors)
  - npm run lint passes (tsc --noEmit)
  - npm test passes: 50 tests (11 + 27 + 12), 3 test files, 0 failures
  - File sizes: context-monitor.ts=97, types.ts=134, index.ts=222 (all under 800)

## Task: P4-CTX-02 — Implement threshold triggers — auto-checkpoint when context > configurable % (DONE)
- Already implemented in P4-CTX-01 (src/monitors/context-monitor.ts) — marking as passes=true
- Reads contextThreshold from plugin config (default 70%) at line 76
- When context % exceeds threshold, triggers checkpoint automatically via emit("amcp:checkpoint:requested") at line 98
- Prevents duplicate checkpoints within cooldown period (5 min default) via lastCheckpointTriggerTime + checkpointCooldownMs at lines 77-107
- Logs checkpoint trigger reason to .amcp/checkpoint-log.jsonl via appendCheckpointLog() at lines 37-43, 88-96
- Emits alert via plugin API: emit("amcp:checkpoint:requested", { trigger: "context_threshold", contextPercent }) at line 98
- Verification test at line 652 of context-monitor.test.ts: "checkpoint created when context hits 70%"
- 10 tests specifically cover threshold/cooldown/logging behavior across 3 test suites
- All 22 context-monitor tests pass
- No code changes needed — all requirements already satisfied

## Task: P4-CTX-03 — Implement time-based triggers — checkpoint every N minutes (configurable) (DONE)
- Modified src/monitors/context-monitor.ts (300 lines) to add time-based interval checkpoint triggers
- Reads checkpointIntervalMs from plugin config (default 0 = disabled) — when > 0, triggers periodic checkpoints
- Time-based triggers checked during the regular 30s poll cycle (not a separate timer) for reliable async behavior
- Added ContentHasher interface to src/types.ts (149 lines) — optional dependency for change detection
- Content hash comparison skips checkpoints when workspace hasn't changed since last checkpoint
- Shares cooldown with threshold-based triggers via extracted triggerCheckpoint() helper function
- Poll chaining pattern (pollChain = pollChain.then(() => poll())) serializes async callbacks, preventing concurrency issues
- Added waitForPendingPoll() test helper on service for reliable fake timer testing
- status() reports intervalEnabled, checkpointIntervalMs, lastCheckpointContentHash
- Logs "time-based checkpoints enabled (every Nmin)" on start when configured
- Created src/monitors/context-monitor-interval.test.ts (472 lines) with 13 dedicated interval tests:
  - Disabled when checkpointIntervalMs=0, triggers at configured interval, multiple intervals
  - Hash-based skip when no changes, trigger after content change, cooldown enforcement
  - Works without contentHasher (always triggers), writes to checkpoint-log.jsonl
  - status() reflects config, logs enabled message, shared cooldown with threshold triggers
  - No triggers after stop(), P4-CTX-03 verification test (3 checkpoints at 1-min intervals)
- Split context-monitor.test.ts to stay under 800 lines (690 lines after split)
- Verification:
  - npm run lint (tsc --noEmit) passes with zero errors
  - npm test passes: 73 tests (22 + 13 + 27 + 11), 4 test files, 0 failures
  - File sizes: context-monitor.ts=300, context-monitor-interval.test.ts=472, context-monitor.test.ts=690, types.ts=149 (all under 800)

## Task: P4-CTX-04 — Implement value-based triggers — checkpoint when high-value content detected (DONE)
- Created `src/monitors/value-monitor.ts` (239 lines) — monitors memory files for high-value content changes
- 6 built-in high-value patterns (BUILTIN_PATTERNS):
  - identity_change: "aid", "publicKey", "kel", "nextKeyHash" in JSON
  - secret_api_key: sk-*, ghp_*, solvr_*, am_* patterns
  - secret_jwt: JWT tokens (eyJ...)
  - secret_bot_token: Telegram bot tokens ([0-9]+:AA...)
  - important_decision: DECISION:, IMPORTANT:, CRITICAL:, BREAKING CHANGE:, NEVER:, ALWAYS: markers
  - soul_change: north star, identity, principles, voice, who I am patterns
- File change detection via FileWatcher interface: getFileHashes() returns SHA-256 hashes, readFile() for content
- Poll loop every 30s: compare hashes to previous poll, scan changed files for patterns
- Initial startup captures baseline hashes WITHOUT triggering checkpoint (avoids false positive on first scan)
- Secret patterns redacted in log summaries ([REDACTED]) — no credentials leak to checkpoint-log.jsonl
- Non-secret patterns show content summary truncated to 80 chars
- Supports custom extra patterns via ValueMonitorDeps.extraPatterns
- Cooldown enforcement shared with checkpoint system (config.checkpointCooldownMs)
- Checkpoint event: emit("amcp:checkpoint:requested", { trigger: "value_content", detections, fileCount })
- Sequential poll chaining pattern (same as context-monitor): pollChain.then(() => poll())
- waitForPendingPoll() helper for deterministic fake-timer testing
- Added types to `src/types.ts` (193 lines): ValuePattern, ValueDetection, FileWatcher, ValueMonitorDeps
- Wired into `src/index.ts` (277 lines):
  - Default watch paths: ~/.amcp/identity.json, SOUL.md, MEMORY.md, USER.md, AGENTS.md, TOOLS.md
  - defaultFileWatcher() reads files from disk using SHA-256 hashing (lazy imports)
  - Gateway can provide custom fileWatcher and watchPaths via extended PluginApi
  - Registered as "amcp-value-monitor" service between context-monitor and memory-monitor
- Created `src/monitors/value-monitor.test.ts` (399 lines) — 20 tests:
  - Pattern scanning (8 tests): identity changes, API keys, JWT tokens, bot tokens, decisions, soul changes, no match, truncation
  - Service lifecycle (12 tests): exports, initial baseline no trigger, identity.json change triggers checkpoint, no trigger when no patterns match, cooldown enforcement, trigger after cooldown, log with redacted secrets, multiple files simultaneously, custom patterns, missing files graceful, status details, no polling after stop
- Updated `src/integration.test.ts`: service count 2→3 in 3 assertions (value-monitor added)
- Exported createValueMonitor from index.ts
- Verification:
  - npm run build (tsc) passes with zero errors
  - npm test passes: 93 tests (22 + 13 + 27 + 20 + 11), 5 test files, 0 failures
  - File sizes: value-monitor.ts=239, value-monitor.test.ts=399, types.ts=193, index.ts=277 (all under 800)

## Task: P4-ID-01 — Implement system-level KERI identity validation (DONE)
- Created `src/identity-manager.ts` (451 lines) — KERI identity validation for the plugin layer
- `validateIdentity(path?)`: loads ~/.amcp/identity.json, validates AID format, public key, KEL chain
  - AID validation: must match KERI format "B" + base64url(Ed25519 public key), 44-45 chars
  - Public key validation: non-empty, base64/base64url encoded, 40-50 char range
  - KEL chain validation: first event must be "inception" with matching AID, subsequent events "rotation"
  - Pre-rotation commitment detection: warns when nextKeyHash present (full hash verification requires amcp CLI)
  - Legacy identity rejection: sha256 hex AIDs (64-char hex) flagged as invalid
  - Secret detection: warns if pinata_jwt, solvr_api_key, etc. found in identity.json
- `getIdentity(logger, path?)`: loads and validates identity, logs errors/warnings, returns AmcpIdentity or null
- `createIdentityService(deps)`: plugin service with start/stop/status/getIdentity/isValid
  - On start: validates identity, emits `amcp:identity:validated` or `amcp:identity:invalid`
  - Invalid identity: logs "checkpoints blocked until resolved", emits alert
  - status() reports: running, identityValid, aid, errors
- Returns `IdentityValidationResult`: { valid, identity, errors[], warnings[] }
- Integrated into `src/index.ts` (320 lines):
  - Imports createIdentityService, registers as "amcp-identity-manager" service
  - Identity path configurable via gateway-provided amcpIdentityPath
  - Exported: validateIdentity, getIdentity, createIdentityService from index.ts
- Created `src/identity-manager.test.ts` (469 lines) — 22 vitest tests:
  - validateIdentity: valid KERI identity, missing file, invalid JSON, no B prefix, short AID
  - validateIdentity: sha256 legacy rejection, missing AID, missing publicKey, secrets warning
  - validateIdentity: no KEL (valid), empty KEL (valid), wrong inception type, mismatched inception AID
  - validateIdentity: inception + rotation chain, non-rotation after inception
  - getIdentity: valid file, invalid file, secrets warning logged
  - createIdentityService: valid identity start, invalid identity alert, legacy sha256 alert, clean stop
- Updated `src/integration.test.ts`: service count 3→4 in 3 assertions (identity-manager added)
- Verification:
  - npm run build (tsc) passes with zero errors
  - npm test passes: 115 tests (22 + 22 + 13 + 27 + 20 + 11), 6 test files, 0 failures
  - File sizes: identity-manager.ts=451, identity-manager.test.ts=469, index.ts=320, integration.test.ts=414 (all under 800)
  - Structural grep: validateIdentity (10 refs), getIdentity (5 refs), createIdentityService (4 refs)

## Task: P4-ID-02 — Handle pre-rotation keys at plugin level (DONE)
- Created `src/key-rotation.ts` (381 lines) — pre-rotation key handling module:
  - `readNextKeyHash(identity)`: reads nextKeyHash from top-level field or last KEL event's next/nextKeyHash
  - `detectRotationNeed(identity, opts)`: checks for compromise flag, scheduled rotation (maxAgeDays, default 90)
  - `rotateKey(trigger, opts)`: validates identity, verifies nextKeyHash exists, calls `amcp identity rotate`,
    validates post-rotation identity, appends rotation record to key-rotations.jsonl
  - `loadRotationHistory(path)`: reads append-only JSONL rotation log
  - `createKeyRotationService(deps)`: plugin service with start/stop/status, checkRotationNeed(), rotate(), getHistory()
  - Emits events: amcp:rotation:needed, amcp:rotation:completed, amcp:rotation:failed
- Updated `src/types.ts` (217 lines):
  - Added `nextKeyHash?: string` to AmcpIdentity interface
  - Added `RotationTrigger` type: "compromise" | "scheduled" | "manual"
  - Added `RotationRecord` interface: timestamp, trigger, previousAid, newAid, previousPublicKey, newPublicKey, kelSequenceNumber, nextKeyHash
- Updated `src/identity-manager.ts` (465 lines):
  - validateIdentity() now extracts nextKeyHash from identity.json (top-level or KEL event next/nextKeyHash fields)
  - Parsed identity includes nextKeyHash in the returned AmcpIdentity object
- Updated `src/index.ts` (339 lines):
  - Imports and registers createKeyRotationService as "amcp-key-rotation" service
  - Exports: readNextKeyHash, detectRotationNeed, rotateKey, loadRotationHistory, createKeyRotationService
- Created `src/key-rotation.test.ts` (575 lines) — 23 vitest tests:
  - readNextKeyHash: top-level field, KEL "next" field, absent (null)
  - validateIdentity nextKeyHash extraction: top-level, KEL "next", absent (undefined)
  - detectRotationNeed: compromise trigger, scheduled (old identity), fresh (no need), compromise priority, default 90d
  - rotateKey: invalid identity, missing nextKeyHash, missing CLI, successful rotation with mock amcp CLI
  - loadRotationHistory: nonexistent file, empty file, multi-line JSONL
  - createKeyRotationService: start + status, rotation:needed for old identity, checkRotationNeed, getHistory, stop
- Updated `src/integration.test.ts`: service count 4→5 in 3 assertions (key-rotation service added)
- Verification:
  - tsc --noEmit passes with zero errors
  - vitest run passes: 138 tests, 7 test files, 0 failures
  - File sizes: key-rotation.ts=381, key-rotation.test.ts=575, types.ts=217, identity-manager.ts=465, index.ts=339 (all under 800)
  - Rotation history stored in ~/.amcp/key-rotations.jsonl (append-only JSONL)
  - amcp:identity:invalid emitted on invalid identity (3 refs in tests)

## P4-ID-03: identity — Auto-inject identity on resurrection (DONE)
- Created `src/monitors/resurrection-identity.ts` (280 lines) — plugin-level identity injection on resurrection
  - loadCheckpointIdentity(): loads identity from last-checkpoint.json (embedded AID/publicKey) or falls back to on-disk identity
  - verifyIdentityMatch(): compares current identity AID with checkpoint identity AID, flags mismatch
  - verifyAndInjectIdentity(): full workflow: detect recent recovery → validate current identity → load checkpoint identity → verify match → emit events
  - isRecentRecovery(): checks if last-recovery.json timestamp is within threshold (default 5 minutes)
  - createResurrectionIdentityService(): plugin service with start/stop/status/verifyNow/getLastResult
  - On start: auto-checks for recent recovery and injects identity if found
  - Events emitted: amcp:identity:injected (success), amcp:identity:mismatch (AID mismatch alert), amcp:identity:invalid (current identity broken)
- Created `src/monitors/resurrection-identity.test.ts` (354 lines) — 12 vitest tests:
  - verifyAndInjectIdentity: skips when no recovery, skips old recovery, skips failed recovery
  - verifyAndInjectIdentity: verifies + injects on matching checkpoint, alerts on AID mismatch, injects current when no checkpoint identity
  - verifyAndInjectIdentity: errors on invalid current identity, respects custom threshold
  - createResurrectionIdentityService: start/stop lifecycle, auto-inject on recent recovery, verifyNow on-demand, status includes verification details
- Updated `src/index.ts` (360 lines):
  - Imports and registers createResurrectionIdentityService as "amcp-resurrection-identity" service
  - Exports: verifyAndInjectIdentity, createResurrectionIdentityService
- Updated `src/integration.test.ts` (416 lines): service count 5→6 in 3 assertions
- Verification:
  - tsc --noEmit passes with zero errors
  - vitest run passes: 150 tests, 8 test files, 0 failures
  - All 5 PRD steps verified via structural grep
  - File sizes: resurrection-identity.ts=280, resurrection-identity.test.ts=354, index.ts=360, integration.test.ts=416 (all under 800)

## Task (P4-ID-04): Support multiple agent identities (DONE)
- Created `src/multi-identity.ts` (411 lines) — multi-identity manager at plugin level
  - `listIdentities()`: reads all stored identities from ~/.amcp/identities/<aid>/identity.json
  - `getActiveAid()`: reads active-identity marker, falls back to main identity.json
  - `storeIdentity()`: copies validated identity to identities/<aid>/ directory
  - `switchIdentity()`: copies target identity to main path, updates active-identity marker
  - `getCheckpointPathForAid()`: returns per-identity last-checkpoint.json path
  - `validateCheckpointOwnership()`: compares checkpoint AID to expected AID, blocks cross-identity resurrection
  - `createMultiIdentityService()`: plugin service with start/stop/status, identity switching, cross-identity guard
- Created `src/multi-identity.test.ts` (532 lines) — 25 tests covering:
  - listIdentities: empty dir, nonexistent dir, lists stored identities, skips invalid
  - getActiveAid: reads marker, falls back to identity.json, returns null when neither exists
  - storeIdentity: stores valid identity, returns null for invalid, provides per-identity checkpoint path
  - switchIdentity: switches to stored identity, updates marker + main path, returns null for nonexistent
  - getCheckpointPathForAid: returns correct per-identity path, different per agent
  - validateCheckpointOwnership: allows matching AID, blocks mismatched AID, handles nested identity.aid, allows legacy (no AID), allows missing checkpoint
  - createMultiIdentityService: start/stop lifecycle, identity switching with events, cross-identity resurrection blocked, error on nonexistent switch, graceful start without identity
- Updated `src/index.ts` (382 lines):
  - Imports and registers createMultiIdentityService as "amcp-multi-identity" service
  - Reads amcpIdentitiesDir from PluginApi extension, defaults to ~/.amcp/identities
  - Exports: listIdentities, getActiveAid, storeIdentity, switchIdentity, getCheckpointPathForAid, validateCheckpointOwnership, createMultiIdentityService
- Updated `src/integration.test.ts` (417 lines): service count 6→7 in 3 assertions, added amcp-multi-identity name check
- Verification:
  - tsc --noEmit passes with zero errors
  - vitest run passes: 175 tests, 9 test files, 0 failures
  - All 5 PRD steps verified: store by AID (identities/<aid>/), CLI switching (switchIdentity), per-identity checkpoint history (getCheckpointPathForAid), cross-identity prevention (validateCheckpointOwnership), multi-agent works (25 tests pass)
  - File sizes: multi-identity.ts=411, multi-identity.test.ts=532, index.ts=382, integration.test.ts=417 (all under 800)

## Task P4-RES-01: resurrection-detector — Detect context wipe at plugin level (DONE)
- Created `src/monitors/resurrection-detector.ts` (260 lines) — monitors SessionApi for context wipe/compaction
  - Polls session context usage at configurable interval (default 10s)
  - Detects sudden context drop >50% reduction between consecutive readings
  - Detects session restart (context drops to 0 from meaningful amount)
  - MIN_TOKENS_FOR_DROP = 1000 (avoids false positives from idle sessions)
  - Logs detections to `~/.amcp/resurrection-log.jsonl` (append-only JSONL)
  - Emits `amcp:resurrection:detected` event with type, tokens, dropPercent
  - Configurable: dropThreshold (default 0.5), pollIntervalMs (default 10_000)
  - Respects `config.resurrection.autoDetect` toggle
  - Graceful error handling: sessionApi failures logged, never crash
  - Exports `createResurrectionDetector()` factory with start/stop/status/waitForPendingPoll/getDetections
- Created `src/monitors/resurrection-detector.test.ts` (506 lines) — 11 tests covering:
  - Service start/stop lifecycle
  - Baseline establishment on first poll (no detection)
  - Sudden context drop >50% detected with correct event emission
  - Session restart (to 0 tokens) detected as "session_restart" type
  - Small changes (<50%) ignored
  - Low token drops (<1000 previous) ignored (avoids false positives)
  - autoDetect=false disables monitoring
  - Custom drop threshold support
  - SessionApi error handling (graceful, logs error)
  - Status includes detection details
  - Context wipe detected within 1 poll cycle
- Updated `src/index.ts` (397 lines):
  - Imports and registers createResurrectionDetector as "amcp-resurrection-detector" service
  - Reads amcpResurrectionLogPath from PluginApi extension, defaults to ~/.amcp/resurrection-log.jsonl
  - Passes sessionApi to detector (shared with context-monitor)
  - Exports createResurrectionDetector
- Updated `src/integration.test.ts`: service count 7→8 in 3 assertions, added amcp-resurrection-detector name check
- Verification:
  - tsc --noEmit passes with zero errors
  - vitest run passes: 186 tests, 10 test files, 0 failures
  - All 6 PRD steps verified: create resurrection-detector.ts ✓, hook into session events ✓, detect >50% drop ✓, detect session restart ✓, log to resurrection-log.jsonl ✓, context wipe detected within 1 poll cycle ✓
  - File sizes: resurrection-detector.ts=260, resurrection-detector.test.ts=506, index.ts=397 (all under 800)

## Task: P4-RES-03 — Auto-select best checkpoint for recovery (DONE)
- Created `src/monitors/checkpoint-selector.ts` (468 lines) — checkpoint scoring and selection engine
- Scoring algorithm with 3 weighted dimensions:
  - Recency (40%): exponential decay with 24-hour half-life
  - Completeness (35%): presence of key metadata fields (CID, timestamp, size, secrets, ontology, soul, type, localPath)
  - Integrity (25%): valid CID format, valid timestamp, structural soundness; corrupted checkpoints get 90% penalty
- `gatherCheckpoints()`: scans 3 sources — per-identity, global last-checkpoint.json, checkpoints dir — deduplicates by CID
- `scoreCheckpoint()`: scores a single candidate, returns ScoredCheckpoint with score breakdown
- `selectBestCheckpoint()`: gathers + scores + selects best non-corrupted; falls back to corrupted if no clean exist
- `listScoredCheckpoints()`: returns all candidates sorted by score (for history/debugging)
- Manual CID override via `manualCid` option — bypasses scoring, returns exact match or null
- Created `src/monitors/checkpoint-selector.test.ts` (571 lines) — 21 tests covering:
  - Score computation (recent vs old, complete vs incomplete, corrupted marking)
  - CID fallback fields (pinataCid, solvrCid)
  - Gathering from all 3 sources + deduplication
  - Best selection with highest score
  - Fallback to older checkpoint when latest is corrupted
  - Corrupted-only recovery (last resort)
  - Manual CID override (found and not found)
  - Score breakdown in list output
- Updated `src/monitors/recovery-prompt-injector.ts`: integrated selectBestCheckpoint into doInject(), added checkpointsDir and manualCid deps
- Updated `src/index.ts`: exported selectBestCheckpoint, listScoredCheckpoints, scoreCheckpoint, gatherCheckpoints
- Verification:
  - tsc --noEmit passes with zero errors
  - vitest run passes: 227 tests, 12 test files, 0 failures
  - All 5 PRD steps verified: scoring (recency+completeness+integrity) ✓, highest score selected ✓, fallback to older on corruption ✓, manual CID override ✓, best checkpoint selected in tests (21 passing) ✓
  - File sizes: checkpoint-selector.ts=468, checkpoint-selector.test.ts=571, recovery-prompt-injector.ts=489, index.ts=421 (all under 800)
