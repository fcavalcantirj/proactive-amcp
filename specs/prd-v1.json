[
  {
    "category": "validation",
    "description": "Add identity pre-flight check to all scripts — call 'amcp identity validate' before operating, fail fast",
    "steps": [
      "Add pre-flight function: run 'amcp identity validate' and capture exit code + errors",
      "Call pre-flight at top of watchdog.sh before entering monitoring loop",
      "Call pre-flight at top of checkpoint.sh before creating tarball",
      "Call pre-flight at top of resuscitate.sh before attempting resurrection",
      "On failure: print 'FATAL: Invalid AMCP identity — run amcp identity create or amcp identity validate for details' and exit 1",
      "Verify: scripts refuse to start when ~/.amcp/identity.json contains openclaw-deploy-style fake identity"
    ],
    "passes": true
  },
  {
    "category": "interface",
    "description": "Create 'proactive-amcp init' — interactive setup: validate/create identity, start watchdog + checkpoint services",
    "steps": [
      "Create proactive-amcp init script/command",
      "Step 1: Check if ~/.amcp/identity.json exists and is valid (via amcp identity validate)",
      "Step 2: If no identity, run 'amcp identity create --out ~/.amcp/identity.json'",
      "Step 3: Set up watchdog systemd service (or cron fallback)",
      "Step 4: Set up checkpoint cron schedule",
      "Step 5: Write config to ~/.amcp/config.json (secrets, intervals)",
      "Verify: running 'proactive-amcp init' on fresh VM produces valid identity + running services"
    ],
    "passes": true
  },
  {
    "category": "secrets",
    "description": "Add 'proactive-amcp config set/get' for ~/.amcp/config.json — all secrets and notify targets read from here, never identity.json",
    "steps": [
      "Create 'proactive-amcp config set <key> <value>' command that writes to ~/.amcp/config.json",
      "Create 'proactive-amcp config get <key>' command that reads from ~/.amcp/config.json",
      "Migrate checkpoint.sh and full-checkpoint.sh to read PINATA_JWT from config.json (already do via .pinata.jwt)",
      "Migrate notify.sh to read notifyTarget and emailTo from config.json instead of ~/.openclaw/openclaw.json skill config",
      "Add warning if scripts detect secrets in identity.json: 'WARNING: Secrets found in identity.json — migrate with proactive-amcp config set'",
      "Verify: grep confirms zero scripts read secrets from identity.json, notify.sh reads from config.json"
    ],
    "passes": true
  },
  {
    "category": "interface",
    "description": "Create 'proactive-amcp install' — non-interactive interface for fleet tools like openclaw-deploy",
    "steps": [
      "Create 'proactive-amcp install' command that wraps init with CLI flags for scripted use",
      "Accept --pinata-jwt <jwt> flag — writes to config.json via config set",
      "Accept --notify-target <target> flag — writes to config.json via config set (notify.sh reads from here after task #3 migration)",
      "Accept --watchdog-interval <seconds> flag — sets watchdog check frequency (default: 120s)",
      "Accept --checkpoint-schedule <cron> flag — sets checkpoint cron expression (default: daily)",
      "Exit 0 on success, exit 1 on failure with structured error output",
      "Verify: 'proactive-amcp install --pinata-jwt xxx --watchdog-interval 60' runs non-interactively"
    ],
    "passes": true
  },
  {
    "category": "validation",
    "description": "Add checkpoint pre-validation — scan tarball for cleartext secrets, refuse to upload if found",
    "steps": [
      "Before tar creation in checkpoint.sh, build file list to be archived",
      "Scan file contents for patterns: JWT tokens (eyJ...), bot tokens ([0-9]+:AA...), API keys",
      "If secrets detected: print 'REFUSED: Checkpoint contains cleartext secrets in: <files>' and exit 1",
      "Add --force flag to bypass check (with loud warning) for emergency checkpoints",
      "Verify: checkpoint with secrets-contaminated identity.json is refused",
      "Verify: checkpoint with clean identity.json + config.json secrets excluded succeeds"
    ],
    "passes": true
  },
  {
    "category": "testing",
    "description": "Integration test: fake openclaw-deploy-style identity.json → all scripts refuse to operate",
    "steps": [
      "Create test fixture: fake identity.json matching openclaw-deploy format (sha256 aid, flat secrets, no kel)",
      "Test: watchdog.sh with fake identity exits 1 with 'Invalid AMCP identity' error",
      "Test: checkpoint.sh with fake identity exits 1 before creating tarball",
      "Test: resuscitate.sh with fake identity exits 1 before attempting resurrection",
      "Test: 'proactive-amcp init' with fake identity replaces it via 'amcp identity create'",
      "Test: 'proactive-amcp config set' never writes to identity.json"
    ],
    "passes": true
  },
  {
    "category": "solvr",
    "description": "Auto-register child Solvr account on first boot — deterministic protocol-08 naming",
    "steps": [
      "Check if SOLVR_API_KEY exists in OpenClaw config or environment — if yes, already registered, done",
      "If missing, check for parentSolvrName in config (skills.entries.proactive-amcp.config.parentSolvrName)",
      "If parentSolvrName exists (child): build name {parentSolvrName}_child_{instance_name}, POST /v1/agents/register",
      "Store returned api_key in OpenClaw config via gateway config.patch",
      "If parentSolvrName missing (root): log warning, disable proactive-solvr features until manual registration",
      "Verify: child auto-registers with correct naming, root does NOT auto-register"
    ],
    "passes": true
  },
  {
    "category": "diagnose",
    "description": "Add 'proactive-amcp diagnose' — Claude-powered health diagnostics with Solvr integration",
    "steps": [
      "Create scripts/claude-diagnose.sh: runs diagnose.sh bash checks, invokes Claude CLI with Solvr skill context",
      "Add 'diagnose' subcommand to proactive-amcp.sh CLI dispatcher",
      "Read ANTHROPIC_API_KEY and SOLVR_API_KEY from env or ~/.amcp/config.json",
      "Build prompt with health findings + Solvr CLI path, invoke claude --dangerously-skip-permissions --no-session-persistence",
      "Claude searches Solvr for each finding, tries fixes, posts novel issues, returns enriched JSON",
      "Support --bash-only (skip Claude), --no-solvr (skip Solvr), --json (raw output) flags",
      "Verify: proactive-amcp diagnose outputs JSON with solvr_searches and fixes_attempted fields"
    ],
    "passes": true
  },
  {
    "category": "diagnose",
    "description": "Bundle Solvr skill with proactive-amcp and install during fleet setup",
    "steps": [
      "Copy Solvr skill files (SKILL.md, solvr.sh, skill.json, api.md, examples.md) to skills/solvr/",
      "install.sh fetches latest via curl -sL solvr.dev/install.sh | bash, falls back to bundled copy",
      "claude-diagnose.sh prefers installed skill at ~/.claude/skills/solvr/, falls back to bundled",
      "Verify: skills/solvr/scripts/solvr.sh exists and is executable",
      "Verify: install.sh installs Solvr skill to ~/.claude/skills/solvr/"
    ],
    "passes": true
  },
  {
    "category": "diagnose",
    "description": "Store anthropic.apiKey in proactive-amcp config for diagnose command",
    "steps": [
      "config.sh already supports arbitrary dot-paths — no changes needed",
      "openclaw-deploy master-setup.sh stores ANTHROPIC_API_KEY via proactive-amcp config set anthropic.apiKey",
      "claude-diagnose.sh reads ANTHROPIC_API_KEY from env or config.json",
      "Verify: proactive-amcp config get anthropic.apiKey returns stored key"
    ],
    "passes": true
  },
  {
    "category": "bugfix",
    "description": "Fix full-checkpoint.sh staging to exclude secret-bearing config files — stop copying ~/.amcp/config.json, secrets-full.json, cache, openclaw.json, auth-profiles.json into staging content",
    "steps": [
      "Replace rsync of ~/.amcp/ (line 322) with targeted copy: only copy identity.json into $STAGING_DIR/amcp/ (secrets are already handled by --secrets flag to amcp checkpoint create)",
      "Remove cp of ~/.openclaw/openclaw.json into staging (line 327) — secrets are extracted by extract_all_secrets(), raw config should not be in content",
      "Remove cp of ~/.openclaw/auth-profiles.json into staging (line 328) — tokens are extracted by extract_all_secrets()",
      "Add config metadata extraction: write non-secret structural metadata to $STAGING_DIR/openclaw/config-metadata.json using jq or python3 (agent names, skill names, gateway port, channel types — no secret values)",
      "Verify: staging directory contains amcp/identity.json but NOT amcp/config.json, amcp/secrets-full.json, amcp/cache/, openclaw/openclaw.json, or openclaw/auth-profiles.json",
      "Verify: pre-validation (scan_for_secrets) passes without --force on staging directory that previously required --force"
    ],
    "passes": true
  },
  {
    "category": "bugfix",
    "description": "Add config-metadata.json extraction — capture non-secret config structure for recovery without exposing credentials",
    "steps": [
      "Create extract_config_metadata() function in full-checkpoint.sh that reads ~/.openclaw/openclaw.json and outputs redacted structural metadata",
      "Metadata must include: gateway.port, gateway.bind, channels (names + enabled status, NOT tokens), skills.entries (names + enabled status, NOT apiKeys), agents.defaults.workspace path",
      "Metadata must NOT include: any field matching secret patterns (jwt, token, apiKey, password, secret, mnemonic, key), channel botToken values, auth profile credentials",
      "Write output to $STAGING_DIR/openclaw/config-metadata.json",
      "Verify: config-metadata.json passes scan_for_secrets with zero detections",
      "Verify: config-metadata.json contains skill names and gateway port but no secret values"
    ],
    "passes": true
  },
  {
    "category": "testing",
    "description": "Integration tests for staging exclusion fix — full-checkpoint succeeds without --force when config files contain secrets",
    "steps": [
      "Test: full-checkpoint.sh with secrets in ~/.amcp/config.json and ~/.openclaw/openclaw.json succeeds without --force (exit 0, no REFUSED message)",
      "Test: full-checkpoint.sh still refuses if workspace content (e.g., workspace/MEMORY.md) contains a hardcoded API key (exit 1, REFUSED message)",
      "Test: staging directory after STAGE 2 contains amcp/identity.json but NOT amcp/config.json or amcp/secrets-full.json",
      "Test: staging directory after STAGE 2 contains openclaw/config-metadata.json but NOT openclaw/openclaw.json or openclaw/auth-profiles.json",
      "Test: config-metadata.json contains gateway port and skill names but zero values matching secret patterns",
      "Test: --force flag still works and produces loud warning when workspace content has secrets"
    ],
    "passes": true
  },
  {
    "category": "ontology",
    "description": "Include memory/ontology/ in full-checkpoint.sh — checkpoint ontology graphs alongside flat files",
    "steps": [
      "Locate staging content copy in full-checkpoint.sh (around line 300-330, STAGE 2: 'Prepare staging content')",
      "Current state: copies $CONTENT_DIR (workspace) to $STAGING_DIR/workspace/, copies amcp/identity.json, openclaw/config-metadata.json",
      "Add conditional copy after workspace rsync: if [ -d \"$CONTENT_DIR/memory/ontology\" ]; then echo '  Included ontology graph ($(find \"$CONTENT_DIR/memory/ontology\" -name '*.jsonl' | wc -l) JSONL files)'; fi",
      "Ontology files already included in workspace rsync — verify they are NOT excluded by scan-secrets.sh (JSONL format should not match secret patterns)",
      "Test: full-checkpoint with memory/ontology/graph.jsonl present includes it in $STAGING_DIR/workspace/memory/ontology/",
      "Verify: unpacking checkpoint restores memory/ontology/ directory structure"
    ],
    "passes": false
  },
  {
    "category": "ontology",
    "description": "Validate ontology on resurrection — create validate-ontology.py, call from resuscitate.sh",
    "steps": [
      "Create scripts/validate-ontology.py (<200 lines) with schema validation for JSONL graph files",
      "Schema rules: each line is valid JSON with 'type' field (entity or relation), entities have id/type/properties/created/updated, relations have from_id/relation_type/to_id",
      "Validation checks: valid JSON per line, required fields present, relation integrity (from_id and to_id reference existing entities), no circular task dependencies (blocks relation must be acyclic)",
      "Output JSON: { valid: true, entity_count: N, relation_count: M } or { valid: false, errors: [...] }",
      "Add validation call in resuscitate.sh after workspace restore (around line 350-400, after 'Decrypt and restore from checkpoint' section)",
      "On validation failure: log warning to $RECOVERY_LOG, do NOT fail resurrection (backward compatible — agents without ontology still resurrect)",
      "Verify: resuscitate with valid ontology passes validation, resuscitate with missing ontology skips validation, resuscitate with corrupt ontology logs warning but completes"
    ],
    "passes": false
  },
  {
    "category": "ontology",
    "description": "Compute ontology graph CID — add ontologyGraphCID field to last-checkpoint.json",
    "steps": [
      "Add compute_ontology_cid() function to full-checkpoint.sh (after extract_all_secrets, around line 200)",
      "If memory/ontology/graph.jsonl exists: compute SHA256 hash, format as CIDv1 using multihash prefix (0x12 = sha256, 0x20 = 32 bytes), base32 encode with 'bafkrei' prefix",
      "Fallback: call 'amcp compute-cid' if CLI supports it, otherwise use python3 with multiformats library (pip install multiformats)",
      "Store in LAST_CHECKPOINT_FILE after checkpoint upload (around line 450-480, where last-checkpoint.json is written)",
      "Field name: ontologyGraphCID (optional, only present if ontology exists)",
      "Test: checkpoint with ontology stores CID in last-checkpoint.json, checkpoint without ontology omits field",
      "Verify: CID is deterministic — same graph content produces same CID across runs"
    ],
    "passes": false
  },
  {
    "category": "ontology",
    "description": "SOUL.md drift detection — store hash, compare on checkpoint, alert on significant changes",
    "steps": [
      "Add compute_soul_hash() function to full-checkpoint.sh (calls sha256sum on $CONTENT_DIR/SOUL.md if exists)",
      "Store soulHash in last-checkpoint.json alongside CID and timestamp (around line 450-480)",
      "On checkpoint: compare current SOUL.md hash to previous (read from last-checkpoint.json)",
      "If different: compute diff using 'diff -u' or python difflib, count changed lines",
      "Severity classification: <5 lines = minor (log), 5-20 lines = moderate (log + optional notify), >20 lines = major (log + notify if configured)",
      "Log to ~/.amcp/soul-drift.log with timestamp, severity, line change count, and diff snippet",
      "If notify.sh exists and notify.enableSoulDrift is true in config.json: send Telegram alert for moderate/major changes",
      "Test: modify SOUL.md by 3 lines, run checkpoint, verify minor drift logged; modify by 25 lines, verify major drift triggers notification",
      "Verify: first checkpoint (no previous hash) does not trigger drift alert"
    ],
    "passes": false
  },
  {
    "category": "ontology",
    "description": "Document reconstruction sequence — create RECONSTRUCTION.md defining canonical loading order",
    "steps": [
      "Create RECONSTRUCTION.md in project root (~150 lines) documenting the Uncanny Seam and memory loading sequence",
      "Define canonical order (references ONTOLOGY-INTEGRATION-CONTEXT.md Part 4.5): 1. SOUL.md (identity), 2. USER.md (relationships), 3. ontology schema (types), 4. ontology graph (entities), 5. MEMORY.md (curated long-term), 6. daily notes (ephemeral), 7. TOOLS.md (capabilities), 8. [SEAM] marker",
      "Explain phenomenological rationale: order matters because each layer provides context for the next, seam is the moment agent stops 'reading about self' and starts 'being self'",
      "Include guidance for agents: 'If you are reading this during resurrection, you are currently between steps 1-7. The seam will fall when you finish loading all context files.'",
      "Reference LumenNox's research (Aclawdemy, 'The Phenomenology of Forgetting')",
      "Add link from main README.md under 'Memory Architecture' section",
      "Verify: RECONSTRUCTION.md passes markdown linting, references are accurate"
    ],
    "passes": false
  },
  {
    "category": "ontology",
    "description": "Typed pruning policy framework — define per-entity-type retention rules in config.json",
    "steps": [
      "Extend config.json schema to support pruning.policies object with per-type rules",
      "Schema: { \"Event\": { \"ttl\": \"30d\", \"preserve_relations\": true }, \"Task\": { \"ttl\": null, \"prune_if\": \"status == 'done' AND updated < 90d\" }, \"Person\": { \"ttl\": null } }",
      "Create scripts/prune-ontology.py (<300 lines) that reads policies from config.json and prunes memory/ontology/graph.jsonl",
      "Pruning logic: parse JSONL, identify entities matching prune criteria, remove entities + optionally relations, write new JSONL (append .pruned, then atomic rename)",
      "Support TTL parsing (30d, 90d, 1y), conditional expressions (status == 'done'), relation preservation (keep relation stubs with deleted_at timestamp)",
      "Add 'prune' subcommand to proactive-amcp.sh CLI dispatcher (dispatches to prune-ontology.py)",
      "Dry-run mode: prune-ontology.py --dry-run outputs what would be pruned without modifying files",
      "Test: define policy pruning Events >30d, run 'proactive-amcp prune --dry-run', verify it identifies correct entities; run without --dry-run, verify entities removed from graph.jsonl",
      "Verify: pruning never removes Person or Task entities with ttl: null, relation stubs preserve graph connectivity after prune"
    ],
    "passes": false
  },
  {
    "category": "testing",
    "description": "Integration tests for ontology checkpoint — verify graph.jsonl roundtrip through IPFS",
    "steps": [
      "Create test/ontology-checkpoint.bats using BATS framework (pattern: test_helper.sh for fixtures + mocks)",
      "Test 1: Create memory/ontology/graph.jsonl with sample entities (2 Person, 1 Task, 1 relation), run full-checkpoint.sh, verify staging includes ontology/",
      "Test 2: Mock Pinata upload, run checkpoint, verify last-checkpoint.json includes ontologyGraphCID field",
      "Test 3: Mock checkpoint download, run resuscitate.sh, verify validate-ontology.py called (check $RECOVERY_LOG for validation output)",
      "Test 4: Corrupt graph.jsonl (invalid JSON on line 3), run resuscitate, verify validation logs warning but resurrection completes",
      "Test 5: Missing ontology directory, run checkpoint, verify no ontologyGraphCID in last-checkpoint.json (backward compatible)",
      "Test 6: CID computation determinism — checkpoint same graph twice, verify identical CID",
      "Run: bats test/ontology-checkpoint.bats",
      "Verify: all 6 tests pass, no false positives"
    ],
    "passes": false
  },
  {
    "category": "testing",
    "description": "Integration tests for SOUL drift — verify identity change alerts",
    "steps": [
      "Create test/soul-drift.bats using BATS framework",
      "Test 1: Initial checkpoint with SOUL.md, verify soulHash stored in last-checkpoint.json, no drift alert",
      "Test 2: Modify SOUL.md (add 3 lines), run checkpoint, verify minor drift logged to ~/.amcp/soul-drift.log, no notification sent",
      "Test 3: Modify SOUL.md (add 25 lines), mock notify.sh, run checkpoint, verify major drift triggers notification (check notify.sh was called with drift message)",
      "Test 4: No SOUL.md present, run checkpoint, verify no soulHash in last-checkpoint.json, no error",
      "Test 5: SOUL.md unchanged across 3 checkpoints, verify no drift alerts logged",
      "Test 6: config.json has notify.enableSoulDrift: false, modify SOUL.md significantly, verify drift logged but no notification sent",
      "Run: bats test/soul-drift.bats",
      "Verify: all 6 tests pass, notification guard works correctly"
    ],
    "passes": false
  },
  {
    "category": "documentation",
    "description": "Update CLAUDE.md and README.md — document ontology integration features",
    "steps": [
      "Update CLAUDE.md Architecture section (around line 20-50) to mention three-layer memory: ontology (structured), AMCP (verified), phenomenological (curated)",
      "Add Ontology section to CLAUDE.md File Map (~50 lines): describe graph.jsonl format, entity types, validation, CID computation, SOUL drift detection, pruning",
      "Update Data Flows section to include ontology validation in checkpoint/resurrection flows",
      "Add new environment variables: ONTOLOGY_DIR (default: $CONTENT_DIR/memory/ontology), SOUL_DRIFT_THRESHOLD (default: 20 lines)",
      "Document 'proactive-amcp prune' subcommand and pruning.policies config schema",
      "Update README.md Features list to include 'Ontology Integration: Typed knowledge graphs with schema validation, identity drift detection, typed pruning policies'",
      "Add RECONSTRUCTION.md to README.md table of contents under 'Memory Architecture'",
      "Reference ONTOLOGY-INTEGRATION-CONTEXT.md in docs/ directory",
      "Verify: documentation is accurate, all file paths correct, no broken internal links"
    ],
    "passes": false
  },
  {
    "category": "ontology-evolution",
    "description": "Implement memory evolution engine — Zettelkasten-style dynamic entity linking based on A-MEM (NeurIPS 2025)",
    "steps": [
      "Create scripts/memory-evolution.sh (<250 lines) — orchestrates evolution engine: validate identity, read graph.jsonl, call similarity computation, update relations, log evolution chain",
      "Create scripts/compute-entity-similarity.py (<200 lines) — semantic similarity using Levenshtein distance + keyword overlap, relation inference based on similarity threshold (>0.75 = strong relation)",
      "Evolution trigger: when new entity X added to graph.jsonl, find top-3 semantically similar entities, add bidirectional 'related_to' relations, log to ~/.amcp/memory/evolution.jsonl",
      "Evolution log format: { timestamp, trigger_entity_id, related_entity_id, similarity_score, relation_type, reasoning }",
      "Integration: call memory-evolution.sh from full-checkpoint.sh after graph staging (around line 330, before CID computation)",
      "Add --skip-evolution flag to full-checkpoint.sh for dry-run checkpoints",
      "Environment variable: EVOLUTION_THRESHOLD (default: 0.75), EVOLUTION_MAX_RELATIONS (default: 3)",
      "Verify: adding Task entity mentioning 'API design' triggers relation updates to existing entities with similar keywords, evolution.jsonl records chain"
    ],
    "passes": false
  },
  {
    "category": "ontology-evolution",
    "description": "Integration tests for memory evolution — verify automatic relation inference",
    "steps": [
      "Create test/memory-evolution.bats using BATS framework, follow test_helper.sh patterns",
      "Test 1: Create graph.jsonl with entity-A (properties: 'API design patterns'), add entity-B ('API architecture') via evolution engine, verify 'related_to' relation added to graph.jsonl",
      "Test 2: Add entity triggering evolution, verify evolution.jsonl contains entry with similarity_score >= 0.75",
      "Test 3: Add entity with no similar entities (threshold <0.75), verify no relations added",
      "Test 4: Evolution threshold EVOLUTION_THRESHOLD=0.9, add moderately similar entity, verify no relation (respects threshold)",
      "Test 5: EVOLUTION_MAX_RELATIONS=1, add entity similar to 5 others, verify only top-1 relation added",
      "Test 6: --skip-evolution flag prevents evolution.jsonl updates",
      "Test 7: Trace evolution chain — entity-C added similar to entity-B which was similar to entity-A, verify chain recorded",
      "Run: bats test/memory-evolution.bats",
      "Verify: all 7 tests pass, relation inference is deterministic and respects thresholds"
    ],
    "passes": false
  },
  {
    "category": "temporal",
    "description": "Implement temporal query interface — cross-checkpoint entity history (Zep pattern, arXiv:2501.13956)",
    "steps": [
      "Create scripts/temporal-queries.py (<250 lines) — temporal query API for entity history across checkpoints",
      "API functions: queryByTimeRange(entity_id, start_date, end_date) -> list of entity versions with timestamps, getMemoryHistory(entity_id) -> full evolution timeline",
      "Implement 3-level temporal storage: Level 1 (current graph.jsonl), Level 2 (checkpoint snapshots in ~/.amcp/checkpoints/), Level 3 (temporal index ~/.amcp/memory/temporal-index.jsonl)",
      "Temporal index format: { entity_id, checkpoint_cid, timestamp, version_hash, properties_snapshot }",
      "Build temporal index during checkpoint: after graph.jsonl staging, extract entity summaries and append to temporal-index.jsonl",
      "Query implementation: scan temporal-index.jsonl for entity_id, filter by time range, fetch full entity from checkpoint CID if needed",
      "Add 'temporal-query' subcommand to proactive-amcp.sh CLI dispatcher: proactive-amcp temporal-query <entity-id> [--start YYYY-MM-DD] [--end YYYY-MM-DD]",
      "Environment variable: TEMPORAL_INDEX_PATH (default: ~/.amcp/memory/temporal-index.jsonl)",
      "Verify: create entity in checkpoint-1, modify in checkpoint-2, query history shows both versions with correct timestamps"
    ],
    "passes": false
  },
  {
    "category": "temporal",
    "description": "Integration tests for temporal queries — verify cross-checkpoint history retrieval",
    "steps": [
      "Create test/temporal-queries.bats using BATS framework",
      "Test 1: Create entity-123 in checkpoint-1 (version 1), modify in checkpoint-2 (version 2), run queryByTimeRange covering both, verify returns 2 versions",
      "Test 2: Query with start_date after checkpoint-1, verify only checkpoint-2 version returned (time filtering works)",
      "Test 3: getMemoryHistory(entity-123) returns full timeline ordered chronologically",
      "Test 4: Entity created but never modified, history shows single entry",
      "Test 5: Non-existent entity_id returns empty list (not error)",
      "Test 6: Temporal index missing (fresh install), queries gracefully degrade to current graph.jsonl only",
      "Test 7: Checkpoint with no ontology does not break temporal index (backward compatible)",
      "Test 8: Query 'what entities existed on 2026-02-15?' returns correct snapshot",
      "Run: bats test/temporal-queries.bats",
      "Verify: all 8 tests pass, cross-checkpoint queries work, time-based filtering accurate"
    ],
    "passes": false
  },
  {
    "category": "contract",
    "description": "Implement ontology contract validation — Design by Contract for skills",
    "steps": [
      "Create scripts/validate-skill-contract.sh (<200 lines) — validates skill ontology contracts on skill load",
      "Create docs/ontology-contract.schema.json (<100 lines) — JSON schema for SkillOntologyContract format",
      "Contract schema: { skill_name: string, version: string, ontology: { reads: EntityType[], writes: EntityType[], preconditions: string[], postconditions: string[] } }",
      "Example contract: { skill_name: 'task-manager', ontology: { reads: ['Task', 'Person'], writes: ['Task', 'Action'], preconditions: ['Task.assignee exists'], postconditions: ['Task.status is valid enum'] } }",
      "Skills declare contracts in skill.json: 'ontologyContract': { ... }",
      "Validation on skill load: check preconditions against current graph.jsonl (e.g., verify all Task entities have assignee property), fail skill load if preconditions not met",
      "Create scripts/detect-contract-conflicts.sh (<150 lines) — cross-skill conflict detection: finds incompatible postconditions across loaded skills",
      "Add 'validate-contract' subcommand to proactive-amcp.sh: proactive-amcp validate-contract <skill-name>",
      "Integration: call validate-skill-contract.sh from OpenClaw skill loader (if proactive-amcp plugin enabled)",
      "Verify: skill declaring 'precondition: Task.assignee exists' fails to load if some Tasks lack assignee, conflict between skill-A postcondition 'Task.status = done' and skill-B postcondition 'Task.status != done' detected"
    ],
    "passes": false
  },
  {
    "category": "contract",
    "description": "Integration tests for contract validation — verify precondition checking and conflict detection",
    "steps": [
      "Create test/skill-contract.bats using BATS framework",
      "Test 1: Skill with precondition 'Task.assignee exists', all Tasks have assignee, validation passes",
      "Test 2: Skill with precondition 'Task.assignee exists', some Tasks missing assignee, validation fails with descriptive error",
      "Test 3: Skill declares reads: ['Task'], graph.jsonl has no Task entities, validation fails (missing required entities)",
      "Test 4: Skill declares writes: ['Action'], after skill execution, verify Action entities created (postcondition verification)",
      "Test 5: Two skills loaded, skill-A postcondition: 'Task.priority is high', skill-B postcondition: 'Task.priority is low', detect-contract-conflicts.sh reports conflict",
      "Test 6: Contract schema validation — invalid contract JSON rejected with schema error",
      "Test 7: Skill without ontologyContract field (backward compatible), validation skipped gracefully",
      "Test 8: Cross-skill reads/writes compatible (skill-A writes Task, skill-B reads Task), no conflict detected",
      "Run: bats test/skill-contract.bats",
      "Verify: all 8 tests pass, precondition enforcement works, conflict detection catches incompatible contracts"
    ],
    "passes": false
  }
]
